# WBC1 Cascade — Описание алгоритма

## 1. Параметры и константы

| Параметр | Значение |
|---|---|
| Размер ключа | 256 бит (32 байта) |
| Раундов в `encrypt_block` | **32** (Perm + Mix + XOR + BRot) |
| Раундов в `derive_cascade_key` | **4** (F+B нелинейное перемешивание) |
| Число операций в таблице | 127 кубических перестановок |
| Размер блока | $d^3$ байт, $d \in \{2,3,4,\ldots,16\}$ → $\{8, 27, 64, 125, 216, \ldots\}$ |
| Размер нонса | 96 бит (12 байт) |
| MAC | HMAC-SHA-256 (32 байта) |

Размер куба выбирается автоматически: наибольшее $d^3 \le \text{len}(\text{data})$.

---

## 2. Вспомогательные функции

### 2.1. Вывод раундового ключа `get_round_key`

Для раунда $r$ и размера куба $B$ байт:

$$
\text{base} = K \;\|\; r_{\text{LE32}}
$$

$$
h_c = \text{SHA-256}(\text{base} \;\|\; c_{\text{LE16}}), \quad c = 1, 2, \ldots
$$

Конкатенируем блоки $h_0, h_1, \ldots$ до получения $\ge B$ байт, берём первые $B$ байт:

$$
\text{RK}[r] = (h_0 \| h_1 \| \ldots)_{[0:B]}
$$

### 2.2. Кубические перестановки `apply_operation`

Куб $C$ — плоский массив из $d^3$ байт в row-major порядке:

$$
\text{idx}(i,j,k) = i \cdot d^2 + j \cdot d + k
$$

Операция $\text{op} \in [0; 126]$ — составная из ($\text{axis}$, $\text{pos}$, $k\!\times\!90°$-поворот слайса) + глобальная перестановка осей. Каждая операция обратима: `apply_operation(cube, op_id, inverse=1)`.

### 2.3. Побитовая ротация `bitwise_rotate_cube`

Каждый байт куба ротируется на `op_id % 8` бит:

$$
\text{byte}_i \leftarrow \text{ROL}_8(\text{byte}_i,\; n), \quad n = \text{op\_id} \bmod 8
$$

Обратная — `ROR_8` на то же $n$.

### 2.4. Диффузия mix_cube / inv_mix_cube

Реализована как **двунаправленный XOR-свёртки** (F-sweep + B-sweep) — полный диффузионный слой.

#### Прямое преобразование `mix_cube`

**Шаг F (вперёд):** XOR prefix sum:

$$
D[i] \;\mathrel{^=}\; D[i-1], \quad i = 1, 2, \ldots, B-1
$$

После F: $D[i] = a_0 \oplus a_1 \oplus \ldots \oplus a_i$, где $a_j$ — исходные байты.

**Шаг B (назад):** XOR suffix reduction:

$$
D[i] \;\mathrel{^=}\; D[i+1], \quad i = B-2, B-3, \ldots, 0
$$

**Итог:** каждый выходной байт $D[i]$ зависит от **всех** $B$ входных байт — полная диффузия за $2(B-1)$ операций XOR (~2 такта/байт).

#### Обратное преобразование `inv_mix_cube`

Применяем шаги в обратном порядке с инверсными операциями:

**Шаг B⁻¹:**

$$
D[i] \;\mathrel{^=}\; D[i+1], \quad i = 0, 1, \ldots, B-2
$$

**Шаг F⁻¹:**

$$
D[i] \;\mathrel{^=}\; D[i-1], \quad i = B-1, B-2, \ldots, 1
$$

#### Обратимость

Каждый из шагов F и B является нижне- (или верхне-) треугольной матрицей над $\mathrm{GF}(2^8)$ с единицами на диагонали, следовательно $\det = 1$ → обратима. Композиция F∘B также обратима.

**Сравнение с однопроходным F-sweep:**

| | Однопроходный (F) | Двунаправленный (F+B) |
|---|---|---|
| $D[0]$ зависит от $a_j$ при $j>0$ | нет | да |
| Полная диффузия за 1 раунд | нет | **да** |
| Операций XOR на блок | $B-1$ | $2(B-1)$ |
| Тактов на байт | ~1 | ~2 |

### 2.5. Деривация каскадного ключа `derive_cascade_key`

Вход: зашифрованный блок $E$ размером $B$ байт. Выход: 32-байтовый ключ для следующего блока.  
Не использует SHA-256; обеспечивает ~50% лавинный эффект за счёт 4 раундов нелинейного перемешивания.

**Шаг 1 — XOR-свёртка** в 64-байтовое состояние $S$:

$$
S[j] = \bigoplus_{k:\; k \bmod 64 = j} E[k], \quad j = 0, \ldots, 63
$$

**Шаг 2 — 4 раунда F+B mixing** (нелинейность через сложение с переносом):

Для каждого раунда $r = 0, 1, \ldots, 15$:

*F-проход (вперёд):*

$$
c_0 = S[63] \oplus r
$$

$$
c_i = S[i] + \text{ROL}_8(c_{i-1}, 5) + (67i + 29r) \bmod 256, \quad S[i] \leftarrow c_i, \quad i = 0, \ldots, 63
$$

*B-проход (назад):*

$$
c_{63} = S[0] \oplus (37r \bmod 256)
$$

$$
c_i = S[i] \oplus \text{ROR}_8(c_{i+1}, 3), \quad S[i] \leftarrow c_i, \quad i = 63, 62, \ldots, 0
$$

Нелинейность достигается через **сложение по модулю 256** (перенос бит не линеен над $\mathrm{GF}(2)$) и константы $67i + 29r$.

**Шаг 3 — выход:**

$$
\text{new\_rk} = S[0 \ldots 31]
$$

Берём первые 32 байта напрямую (без повторной XOR-свёртки, чтобы не терять энтропию).

---

## 3. Иерархия обработки: от текста до раундов

```
Открытый текст
    │
    ▼  разбивка на чанки (по одному на каждый MPI-процесс)
    ├── Чанк 0 (rank 0) ──────────────────────────────────────────┐
    │     │                                                        │
    │     │  стартовый ключ: derive_parallel_key(master, 0)       │
    │     │                                                        │
    │     ├── Блок 0 → encrypt_block(rk0) ─→ inter[0]            │  ПРОХОД 1
    │     │                │                                       │  (вперёд)
    │     │                └─→ derive_cascade_key(inter[0]) → rk1 │
    │     │                                                        │
    │     ├── Блок 1 → encrypt_block(rk1) ─→ inter[1]            │
    │     │                │                                       │
    │     │                └─→ derive_cascade_key(inter[1]) → rk2 │
    │     │    ...
    │     │                                                        │
    │     │  стартовый ключ: derive_parallel_key(master, nproc+0) │
    │     │                                                        │
    │     ├── Блок K-1 → encrypt_block(rk'K) ─→ ct[K-1]         │  ПРОХОД 2
    │     │                │                                       │  (назад)
    │     │                └─→ derive_cascade_key(ct[K-1]) → rk'K-1│
    │     │    ...
    │     └── Блок 0 → encrypt_block(rk'1) ─→ ct[0]             │
    │                                                              │
    ├── Чанк 1 (rank 1) — аналогично, параллельно ───────────────┘
    │    ...
    └── Чанк N-1 (rank N-1)
```

**Ключевое:** каждый `encrypt_block` выполняет **32 раунда** внутри.  
Каждый `derive_cascade_key` выполняет **4 раунда** внутри.  
Это два разных цикла с разными задачами.

---

## 4. `encrypt_block` — шифрование одного блока (32 раунда)

Вход: ключ $K$ (32 байта), блок $P$ (64 байта); выход: блок $C$ (64 байта).

### Развёртка ключа (без SHA-256)

Для $r = 0..31$, $i = 0..63$:

$$
\text{RK}[r][i] = \text{ROL}_8\!\left(K\!\left[(i + 7r) \bmod 32\right],\; r \bmod 8\right)
$$

$$
\text{op\_id}[r] = K[r] \bmod 127
$$

### 32 раунда

```
state ← P

Для r = 0, 1, …, 31:

  1. Perm:  state ← perm_table[op_id[r]][state]      // кубическая перестановка
  2. Mix:   state[i] ^= state[i-1]  (i=1..63)        // F-sweep
            state[i] ^= state[i+1]  (i=62..0)        // B-sweep (полная диффузия)
  3. XOR:   state[i] ^= RK[r][i]    (i=0..63)        // примешиваем ключ
  4. BRot:  state[i] = ROR₈(state[i], op_id[r]%8)   // ротация бит

C ← state
```

Один раунд схематически:

$$
\text{раунд}_r(X) = \text{BRot}\!\left(\text{XOR}_{\text{RK}[r]}\!\left(\text{Mix}\!\left(\text{Perm}_{\text{op\_id}[r]}(X)\right)\right)\right)
$$

### Зачем 32 раунда?

| Раундов | Криптостойкость |
|---------|----------------|
| 1 | Байты слабо перемешаны, видны паттерны |
| 4 | Лучше, но статистические зависимости ещё есть |
| **32** | **Лавинный эффект ~50%, паттерны не видны** |

---

## 5. `decrypt_block` — дешифрование одного блока

Те же 32 раунда в **обратном порядке**, каждый шаг обратный:

```
state ← C

Для r = 31, 30, …, 0:

  4⁻¹. inv BRot:  state[i] = ROL₈(state[i], op_id[r]%8)
  3⁻¹. XOR:       state[i] ^= RK[r][i]          // XOR само-обратно
  2⁻¹. inv Mix:   state[i] ^= state[i+1]  (i=0..62)   // B⁻¹
                  state[i] ^= state[i-1]  (i=63..1)   // F⁻¹
  1⁻¹. inv Perm:  state ← inv_perm_table[op_id[r]][state]

P ← state
```

Корректность:

$$
\text{decrypt\_block}(K,\; \text{encrypt\_block}(K, P)) = P
$$

---

## 6. Дополнение (padding)

Формат: `[данные] [fill_len нулевых байт] [блок дополнения]`

Блок дополнения содержит `fill_len` как 2-байтовое LE-число в первых двух байтах.

- Если $\text{len} \bmod B = 0$ → fill_len = 0
- Иначе fill_len = $B - (\text{len} \bmod B)$

Итого: `len(padded) = ceil(len / B) × B + B`

---

## 7. Двусторонний каскад

### 7.1. Шифрование `cascade_encrypt`

#### Проход 1 (вперёд, прямой)

$$
rk^{(1)}_0 = K_{\text{master}}
$$

$$
\text{inter}[b] = \text{encrypt\_block}(rk^{(1)}_b,\; \text{padded}[b]), \quad b = 0, 1, \ldots, N{-}1
$$

$$
rk^{(1)}_{b+1} = \text{derive\_cascade\_key}(\text{inter}[b])
$$

Режим ECB: блок шифруется с текущим $rk^{(1)}_b$.  
Режим CBC: применяется XOR с предыдущим шифртекстом перед шифрованием.  
Режимы CFB/OFB/CTR: используют IV и шифруют предыдущий блок для генерации ключевого потока.

#### Проход 2 (назад, обратный — только ECB)

Независимый начальный ключ:

$$
rk^{(2)}_N = \text{derive\_cascade\_key}(K_{\text{master}})
$$

Проход в обратном порядке:

$$
\text{ct}[b] = \text{encrypt\_block}(rk^{(2)}_{b+1},\; \text{inter}[b]), \quad b = N{-}1, N{-}2, \ldots, 0
$$

$$
rk^{(2)}_b = \text{derive\_cascade\_key}(\text{ct}[b])
$$

#### Итоговый шифртекст

$$
\text{CT} = \text{ct}[0] \| \text{ct}[1] \| \ldots \| \text{ct}[N-1]
$$

---

### 7.2. Дешифрование `cascade_decrypt`

#### Отмена прохода 2 (назад)

Восстанавливаем $rk^{(2)}$ детерминированно из $K_{\text{master}}$, а ключ обновляется из **шифртекста ct** (известен):

$$
rk^{(2)}_N = \text{derive\_cascade\_key}(K_{\text{master}})
$$

$$
\text{inter}[b] = \text{decrypt\_block}(rk^{(2)}_{b+1},\; \text{ct}[b]), \quad b = N{-}1, \ldots, 0
$$

$$
rk^{(2)}_b = \text{derive\_cascade\_key}(\text{ct}[b])
$$

#### Отмена прохода 1 (вперёд)

Ключ обновляется из **inter** (только что восстановленного):

$$
rk^{(1)}_0 = K_{\text{master}}
$$

$$
\text{pt}[b] = \text{decrypt\_block}(rk^{(1)}_b,\; \text{inter}[b]), \quad b = 0, 1, \ldots, N{-}1
$$

$$
rk^{(1)}_{b+1} = \text{derive\_cascade\_key}(\text{inter}[b])
$$

После снятия дополнения: $P = \text{wbc1\_unpad}(\text{pt})$.

---

## 8. Режим WBC-CTR-HMAC

Полная схема с аутентификацией:

```
nonce ← random 12 байт
(key_crypt, key_mac) ← SHA-512(K || nonce)   // разделение ключей

CT ← cascade_encrypt(key_crypt, data, CTR-режим, nonce)
tag ← HMAC-SHA-256(key_mac, nonce || CT)

output: nonce (12) || CT || tag (32)
```

Дешифрование: проверяем `tag` перед расшифровкой (Encrypt-then-MAC).

---

## 9. Диаграмма двустороннего каскада

```
Открытый текст:  P[0]    P[1]    P[2]    …    P[N-1]
                  │       │       │              │
               pad[0]  pad[1]  pad[2]         pad[N-1]
                  │       │       │              │
  ПРОХОД 1 →   ENC(rk0) ENC(rk1) ENC(rk2)    ENC(rk_{N-1})
                  │       │       │              │
              inter[0] inter[1] inter[2]      inter[N-1]
                  │       │       │              │
  ПРОХОД 2 ←  ENC(rk'2) ENC(rk'1) ENC(rk'0) ← rk'_{N} = derive(K)
                  │       │       │              │
               ct[0]   ct[1]   ct[2]          ct[N-1]
                              (шифртекст)

 rk0 = K_master,  rk_{b+1} = derive(inter[b])
 rk'_N = derive(K_master),  rk'_b = derive(ct[b])
```

---

## 10. Свойства алгоритма

### Лавина (эмпирически, ECB, блок 64 байта)

| Метрика | Значение |
|---|---|
| Глобальный лавинный эффект | ≈ 50% |
| Per-block диффузия (с mix_cube) | ≈ 6% |
| Per-block диффузия (без mix_cube) | ≈ 0.2% |

Двусторонний каскад даёт теоретически близкий к оптимальному глобальный лавинный эффект: изменение одного бита во входе влияет на все блоки в обоих направлениях.

### Энтропия шифртекста

Энтропия Шеннона: ≈ 7.9 бит/байт (близко к максимальным 8.0).

### Обратимость

Каждый компонент алгоритма обратим:
- `apply_operation` / `apply_operation(inverse)` — биективная перестановка bytes на $d^3$
- `mix_cube` / `inv_mix_cube` — нижнетреугольная матрица $L$ над $\text{GF}(2^8)$, $\det=1$
- `XOR rk` — само-обратная при том же ключе
- `bitwise_rotate_cube` / `inv_bitwise_rotate_cube` — обратимы при том же $n$

### Детерминированность каскадных ключей при дешифровании

Ключевое свойство: ключ обновляется из данных, доступных в момент вычисления:
- Проход 2 при дешифровании: ключ обновляется из **шифртекста** `ct[b]` (известен сразу)
- Проход 1 при дешифровании: ключ обновляется из **inter[b]** (восстановлен на предыдущем шаге)

Это гарантирует детерминированное восстановление всей цепочки ключей без дополнительного хранения.

---

## 11. Размеры данных

```
Размер     dim   Блок    Блоков (1000 байт)
8          2     8       125
27         3     27       37
64         4     64       15
125        5     125       8
216        6     216       4
343        7     343       2
512        8     512       1
1000      10    1000       1
```

Автовыбор: `auto_block_size(n) = max{d³ | d³ ≤ n}`.

---

## 12. Сравнение вариантов: PWBC2 и PWBC2.1

PWBC2 и PWBC2.1 решают разные задачи, хотя оба работают с одинаковым набором из 127 кубических перестановок.

**PWBC2** акцентирован на криптостойкости. Его отличительная черта — особый нулевой раунд, в котором блок проходит через 32 последовательных перестановки (по одной на каждый байт 256-битного ключа), каждая из которых сопровождается побитовым сдвигом. Это интенсивно перемешивает данные ещё до первого основного раунда. В последующих раундах к каждому блоку применяется ключезависимый S-box и двухслойная диффузия, что обеспечивает высокую нелинейность. Число раундов можно варьировать от 1 до 100.

После цикла 32 перестановок нулевой раунд завершается тремя дополнительными операциями:

$$
W_t^{(0)} = W_t^{(0)} \oplus RK_0 \quad \text{// XOR с раундовым ключом}
$$
$$
W_t^{(0)} = S\!\left(W_t^{(0)}\right) \quad \text{// S-box по каждому байту}
$$
$$
W_t^{(0)} = D\!\left(W_t^{(0)}\right) \quad \text{// двухслойная диффузия}
$$

Эти три операции завершают нулевой раунд и обоснованы следующим образом. Все 32 перестановки и побитовые сдвиги — линейные биективные преобразования, поэтому без дополнительных шагов нулевой раунд целиком поддавался бы линейному анализу. XOR с $RK_0$ устраняет эту проблему на уровне ключевого материала: без него $RK_0$ вообще не участвовал бы в шифровании, поскольку раунды $1 \ldots N$ используют $RK_1, \ldots, RK_N$ — key whitening здесь выполняет ту же роль, что и в AES или IDEA. S-box затем вводит нелинейность, так что в раунд 1 поступают байты, недоступные для линейного криптоанализа. Наконец, двухслойная диффузия «размазывает» локальные изменения от 32 перестановок по всему блоку: без неё изменение одного байта на выходе нулевого раунда затрагивало бы лишь ограниченную область блока на входе раунда 1, ослабляя лавинный эффект.

**PWBC2.1** решает другую проблему — производительность. Все 127 перестановок вычисляются один раз при инициализации и сохраняются в кеше. При шифровании каждого блока алгоритм просто обращается к готовой таблице вместо того, чтобы каждый раз заново вычислять перестановки через SHA-256. Это даёт ускорение в 10–50 раз. Дополнительно предусмотрен режим SIMPLIFIED, где из пяти операций раунда оставляют только две (перестановка + ротация), жертвуя стойкостью ради максимальной скорости.

| | PWBC2 | PWBC2.1 |
|---|---|---|
| Приоритет | Криптостойкость | Производительность |
| Нулевой раунд | 32 перестановки + 32 сдвига + XOR + S-box + диффузия | Нет особого нулевого раунда |
| Кеш перестановок | Нет | Да (10–50× ускорение) |
| Режимы | — | FULL (5 операций) / SIMPLIFIED (2 операции) |
| Раундов | 1–100 | Фиксированный блок 16 байт |

**Итого:** PWBC2 — когда важна криптостойкость, PWBC2.1 — когда важна скорость.
