# Математическое описание алгоритмов WBC1 и WBC2

## Mathematical Description of WBC1 and WBC2 Algorithms

---

## 1. Обозначения и определения / Notation and Definitions

### 1.1 Математические символы / Mathematical Symbols

- **ℤ** — множество целых чисел / set of integers
- **ℕ** — множество натуральных чисел / set of natural numbers  
- **$\{0,1\}$** — бинарное множество / binary set
- **$\{0,1\}^n$** — множество всех бинарных строк длины $n$ / set of all binary strings of length $n$
- **GF(256)** — конечное поле Галуа порядка 256 / Galois field of order 256
- **$\oplus$** — побитовое XOR / bitwise XOR
- **$\boxplus$** — модульное сложение / modular addition
- **$\circ$** — композиция функций / function composition
- **$|X|$** — длина (размер) $X$ в битах / length of $X$ in bits
- **$|X|_B$** — длина (размер) $X$ в байтах / length of $X$ in bytes
- **Индексация** — в формулах явно указывается битовый или байтовый контекст / indexing context (bit/byte) is stated explicitly in formulas
- **$X \ll k$** — логический сдвиг влево на $k$ бит / left logical shift by $k$ bits
- **$X \gg k$** — логический сдвиг вправо на $k$ бит / right logical shift by $k$ bits
- **$||$** — конкатенация / concatenation

### 1.2 Важное пояснение о нотации {0,1}^n

**Что означает $\{0,1\}^n$:**

Нотация $\{0,1\}^n$ представляет собой стандартное математическое обозначение для множества всех возможных последовательностей из $n$ бит, где каждый бит может принимать значение 0 или 1.

**Примеры:**
- $\{0,1\}^1 = \{0, 1\}$ — два возможных значения (1 бит)
- $\{0,1\}^2 = \{00, 01, 10, 11\}$ — четыре возможных значения (2 бита)
- $\{0,1\}^8 = \{00000000, 00000001, ..., 11111111\}$ — 256 возможных значений (1 байт)
- $\{0,1\}^{256}$ — $2^{256}$ возможных значений (256 бит)

**Почему для S-box используется $\{0,1\}^8$:**

S-box (Substitution box) в алгоритме WBC2 работает на уровне **байтов**, а не отдельных битов. Каждый байт представляет собой 8-битную последовательность, которая может принимать значения от 0 до 255 (всего 256 различных значений).

$$S : \{0,1\}^8 \to \{0,1\}^8$$

означает, что S-box — это функция, которая:
- **Принимает на вход:** один байт (8 бит) — элемент множества $\{0,1\}^8$
- **Выдаёт на выход:** один байт (8 бит) — элемент множества $\{0,1\}^8$
- **Является биекцией:** каждому входному байту соответствует уникальный выходной байт

Это эквивалентно записи $S : \{0, 1, 2, ..., 255\} \to \{0, 1, 2, ..., 255\}$, где S — взаимно однозначное отображение множества байтовых значений на себя.

**Преимущества побайтовой обработки:**
1. **Эффективность:** обработка байтами в 8 раз быстрее, чем побитовая
2. **Простота реализации:** стандартные процессоры работают с байтами
3. **Совместимость:** стандартная практика в криптографии (AES, DES используют байтовые S-box)

### 1.3 Параметры алгоритмов / Algorithm Parameters

- **$n$** — длина текста в битах / text length in bits (общий размер открытого текста)
- **$s$** — размер блока в битах / block size in bits (обычно 64, 128, 256, 512)
- **$k$** — количество блоков / number of blocks ($k = \lceil n/s \rceil$)
- **$k_{\text{key}}$** — размер ключа в битах / key size in bits (обычно 256)
- **$m$** — количество байтов ключа / number of key bytes ($k_{\text{key}}/8$, обычно 32)
- **$r$** — количество раундов / number of rounds (WBC2: по умолчанию 10)
- **$d$** — параметр циклического сдвига / cyclic shift parameter (обычно 4, 8, 16)

### 1.4 Структуры данных / Data Structures

- **$T = \{B_0, B_1, \ldots, B_{k-1}\}$** — открытый текст как набор блоков / plaintext as set of blocks
- **$B_b \in \{0,1\}^s$** — $b$-й блок размером $s$ бит, $b \in \{0,1,\ldots,k-1\}$ / $b$-th block of size $s$ bits
- **$C \in \{0,1\}^n$** — шифротекст / ciphertext (конкатенация зашифрованных блоков)
- **$K \in \{0,1\}^{k_{\text{key}}}$** — мастер-ключ / master key
- **$\text{RK}_i \in \{0,1\}^s$** — $i$-й раундовый ключ / $i$-th round key
- **$S : \{0,1\}^8 \to \{0,1\}^8$** — функция замены (S-box), работающая с байтами
- **$O_i : \{0,1\}^s \to \{0,1\}^s$** — $i$-я операция куба Рубика на блоке / $i$-th Rubik's cube operation on block

### 1.5 Операции / Operations

**Функции шифрования и дешифрования:**

$$E : \{0,1\}^s \times \{0,1\}^{k_{\text{key}}} \to \{0,1\}^s \text{ — функция шифрования блока}$$

$$D : \{0,1\}^s \times \{0,1\}^{k_{\text{key}}} \to \{0,1\}^s \text{ — функция дешифрования блока}$$

**Свойство корректности / Correctness property:** 

$$D(E(B_b,K),K) = B_b \text{ для всех блоков } B_b, \text{ ключа } K$$

**Вспомогательные функции:**

$$\text{mix} : \{0,1\}^8 \to \{0,1\}^8 \text{ — перемешивание байта ключа}$$

$$\rho_d : \{0,1\}^s \to \{0,1\}^s \text{ — циклический побитовый сдвиг блока}$$

$$\text{SHA256} : \{0,1\}^* \to \{0,1\}^{256} \text{ — криптографическая хеш-функция}$$

### 1.6 Разбиение текста на блоки / Text Division into Blocks

Перед началом шифрования исходный текст произвольной длины $n$ бит разбивается на блоки фиксированного размера $s$ бит.

**Математическая формулировка:**

$$T = B_0 || B_1 || B_2 || \cdots || B_{k-1}$$

или в виде множества:

$$T = \{B_0, B_1, B_2, \ldots, B_{k-1}\}$$

где:
- $n$ — длина исходного открытого текста в битах / total text length in bits
- $s$ — размер блока в битах / block size in bits (например, 64, 128, 256, 512)
- $k$ — общее количество блоков / total number of blocks
- $B_b$ — $b$-й блок, $B_b \in \{0,1\}^s$ / $b$-th block
- $||$ — операция конкатенации / concatenation

**Количество блоков:**

$$k = \lceil n / s \rceil$$

где:
- $n$ — длина текста в битах
- $s$ — размер блока в битах
- $\lceil \cdot \rceil$ — округление вверх

**Обработка последнего блока:**

Если $n \bmod s \neq 0$, последний блок дополняется (padding):

$$B_{k-1} = T_{\text{остаток}} || \text{padding}$$

**Пример с s=128 бит:**

Для текста длиной $n = 1000$ бит и размера блока $s = 128$ бит:
$$k = \lceil 1000 / 128 \rceil = 8 \text{ блоков}$$

- Блоки $B_0, B_1, \ldots, B_6$: полные (по 128 бит каждый)
- Блок $B_7$: 104 бита данных + 24 бита padding

**Визуальное представление:**
```
Текст длиной n=1000 бит, блоки размером s=128 бит, k=8 блоков:
┌─────────┬─────────┬─────────┬───┬─────────┬─────────┐
│   B₀    │   B₁    │   B₂    │...│   B₆    │   B₇    │
│ 128 бит │ 128 бит │ 128 бит │   │ 128 бит │ 104+24  │
└─────────┴─────────┴─────────┴───┴─────────┴─────────┘
```

---

## 2. Алгоритм PWBC1.1 — Параллельный каскадный шифр / PWBC1.1 Parallel Cascade Cipher

### 2.0 Иерархия обработки

Алгоритм PWBC1.1 обрабатывает текст на трёх уровнях вложенности:

$$
\underbrace{\text{Текст}}_{\text{n байт}}
\;\xrightarrow{\div p}\;
\underbrace{\text{Чанк}_c}_{\text{K блоков}}
\;\xrightarrow{\times K}\;
\underbrace{\text{Блок}_b}_{\text{B байт}}
\;\xrightarrow{\times 32}\;
\underbrace{\text{Раунд}_r}_{\text{Perm+Mix+XOR+BRot}}
$$

где $p$ — число MPI-процессов, $B = d^3$ байт (например, $d=4 \Rightarrow B=64$).

**Два типа раундов:**

| Уровень | Функция | Раундов | Назначение |
|---------|---------|---------|-----------|
| Блок | `encrypt_block` | **32** | Зашифровать 64 байта: Perm + Mix + XOR + BRot |
| Каскад | `derive_cascade_key` | **4** | Получить ключ для следующего блока |

### 2.1 Параметры / Parameters

- $p$ — число MPI-процессов (чанков)
- $K$ — мастер-ключ, $K \in \{0,1\}^{256}$ (32 байта)
- $B = d^3$ — размер блока в байтах ($d \in \{2,..,16\}$, например $d=4 \Rightarrow B=64$)
- $R = 32$ — число раундов `encrypt_block`
- $R_c = 4$ — число раундов `derive_cascade_key`
- $N$ — число блоков после дополнения

### 2.2 Разбивка на чанки / Chunking

Текст длиной $n$ байт после дополнения состоит из $N$ блоков:

$$N = \left\lceil \frac{n}{B} \right\rceil + 1 \quad \text{(+1 блок дополнения)}$$

Чанк $c$ процесса $c \in \{0,..,p-1\}$ содержит блоки:

$$\text{Chunk}_c = \{B_b \;:\; \lfloor b \cdot p / N \rfloor = c\} \quad \approx \lfloor N/p \rfloor \text{ блоков}$$

Стартовый ключ чанка $c$:

$$rk^{(c)}_0 = \text{DPK}(K, c), \quad rk^{(c,2)}_0 = \text{DPK}(K, p+c)$$

где $\text{DPK}(K, idx)$ — быстрая нелинейная функция деривации ключа по индексу (без SHA-256).

### 2.3 Каскадное шифрование одного чанка / Cascade within chunk

**Проход 1 (вперёд):** для $b = 0, 1, \ldots, K_c - 1$:

$$\text{inter}_b = E(rk^{(c)}_b,\; \text{plain}_b)$$

$$rk^{(c)}_{b+1} = \text{DCK}(\text{inter}_b)$$

**Проход 2 (назад):** для $b = K_c-1, K_c-2, \ldots, 0$:

$$\text{ct}_b = E(rk^{(c,2)}_{b+1},\; \text{inter}_b)$$

$$rk^{(c,2)}_b = \text{DCK}(\text{ct}_b)$$

Здесь $\text{DCK}$ — `derive_cascade_key` (см. §2.5).

### 2.4 `encrypt_block`: 32 раунда / 32-round block cipher

$$E : \{0,1\}^{8B} \times \{0,1\}^{256} \to \{0,1\}^{8B}$$

**Развёртка ключа** (без SHA-256):

$$\text{RK}[r][i] = \text{ROL}_8\!\left(K\!\left[(i + 7r) \bmod 32\right],\; r \bmod 8\right), \quad r \in [0,31],\; i \in [0,B{-}1]$$

$$\text{op\_id}[r] = K[r] \bmod 127$$

**Раунд $r$** над состоянием $X \in \{0,1\}^{8B}$:

$$X \xrightarrow{\text{Perm}} \xrightarrow{\text{Mix}} \xrightarrow{\oplus \text{RK}[r]} \xrightarrow{\text{BRot}} X'$$

где:
- $\text{Perm}$: $X[i] \leftarrow X[\sigma_{\text{op\_id}[r]}(i)]$ — кубическая перестановка (один из 127 Рубик-алгоритмов)
- $\text{Mix F}$: $X[i] \leftarrow X[i] \oplus X[i{-}1]$, $i = 1..B{-}1$ — XOR prefix-sum
- $\text{Mix B}$: $X[i] \leftarrow X[i] \oplus X[i{+}1]$, $i = B{-}2..0$ — XOR suffix-reduce
- $\text{BRot}$: $X[i] \leftarrow \text{ROR}_8(X[i],\; \text{op\_id}[r] \bmod 8)$

**Полная функция шифрования блока:**

$$E(K, P) = \text{Round}_{31} \circ \text{Round}_{30} \circ \cdots \circ \text{Round}_0(P)$$

$$\text{Round}_r(X) = \text{BRot}_r\!\left(\text{XOR}_{\text{RK}[r]}\!\left(\text{Mix}\!\left(\text{Perm}_r(X)\right)\right)\right)$$

### 2.5 `derive_cascade_key`: 4 раунда / 4-round key derivation

$$\text{DCK} : \{0,1\}^{8B} \to \{0,1\}^{256}$$

**Шаг 1** — XOR-свёртка в 64-байтное состояние $S$:

$$S[j] = \bigoplus_{k:\; k \bmod 64 = j} \text{ct}[k], \quad j = 0, \ldots, 63$$

**Шаг 2** — 4 раунда F+B mixing (нелинейность через $\boxplus$):

Для $r = 0, \ldots, 3$:

*F-проход:*
$$c_0 = S[63] \oplus r, \qquad c_i = S[i] \boxplus \text{ROL}_8(c_{i-1}, 5) \boxplus (67i + 29r)_{\bmod 256}, \quad S[i] \leftarrow c_i$$

*B-проход:*
$$c_{63} = S[0] \oplus (37r)_{\bmod 256}, \qquad c_i = S[i] \oplus \text{ROR}_8(c_{i+1}, 3), \quad S[i] \leftarrow c_i$$

**Шаг 3** — вывод:

$$\text{DCK}(\text{ct}) = S[0..31]$$

**Свойство (проверено экспериментально, r=4):** изменение 1 бита во входе → ~50% изменённых бит на выходе:

$$\Pr\!\left[\text{DCK}(x)[i] \neq \text{DCK}(x \oplus e_j)[i]\right] \approx 0.5, \quad \forall i, j$$

### 2.6 Корректность / Correctness

$$\text{decrypt\_block}(K,\; \text{encrypt\_block}(K, P)) = P$$

$$\text{cascade\_decrypt}\!\left(K,\; \text{cascade\_encrypt}(K, T)\right) = T$$

---

## 3. Алгоритм WBC1 (базовая версия) / WBC1 Algorithm (base version)

**Примечание:** далее описание базовой версии WBC1 (без каскада и MPI) для одного блока $B_b$.

### 3.1 Концепция алгоритма

Алгоритм WBC1 (White-Box Cipher 1) представляет собой инновационное решение в области блочного шифрования, которое объединяет математическую элегантность операций трёхмерного куба Рубика с практической эффективностью побайтовой обработки данных.

**Основная идея:** Каждый байт ключа управляет выбором одной из 127 тщательно спроектированных операций перестановки, которые моделируют повороты граней трёхмерного куба. Эти операции последовательно применяются к блоку данных, чередуясь с циклическими побитовыми сдвигами для обеспечения максимальной диффузии.

**Философия дизайна:**
1. **Простота и эффективность:** использование только перестановок и сдвигов — быстрых операций
2. **Ключевая зависимость:** каждый байт ключа напрямую влияет на выбор операции
3. **Диффузия:** циклические сдвиги обеспечивают распространение изменений по всему блоку
4. **Нелинейность:** комбинация операций создаёт сложные нелинейные преобразования

### 3.2 Математическая формулировка

**Функция шифрования определяется как:**

$$E : \{0,1\}^s \times \{0,1\}^{k_{\text{key}}} \to \{0,1\}^s$$

$$E(B_b, K) = \pi(B_b)$$

где $\pi$ — композиция операций, применяемых последовательно:

$$\pi = \rho_d \circ O_{j_{m-1}} \circ \rho_d \circ O_{j_{m-2}} \circ \cdots \circ \rho_d \circ O_{j_1} \circ \rho_d \circ O_{j_0} \circ \rho_d$$

### 3.3 Компоненты алгоритма

#### 3.3.1 Операции куба Рубика

Множество операций определяется как:

$$\mathcal{O} = \{O_0, O_1, \ldots, O_{126}\}$$

где каждая операция $O_i : \{0,1\}^s \to \{0,1\}^s$ представляет собой перестановку (биективное отображение) битов блока.

**Свойства операций:**
- **Биективность:** $O_i$ обратима, существует $O_i^{-1}$ такая, что $O_i^{-1}(O_i(X)) = X$
- **Детерминированность:** одинаковый вход всегда даёт одинаковый выход
- **Нелинейность:** комбинация операций создаёт сложные зависимости

#### 3.3.2 Функция перемешивания байта ключа

Перед использованием байта ключа для выбора операции, он подвергается перемешиванию через функцию mix. Это критически важно для обеспечения хороших криптографических свойств.

$$\text{mix} : \{0,1\}^8 \to \{0,1\}^8$$

$$\text{mix}(b) = b \oplus (b \gg 4) \oplus ((b \ll 3) \lor (b \gg 5)) \oplus (b \gg 1)$$

где:
- $\oplus$ — побитовое исключающее ИЛИ (XOR)
- $\gg k$ — логический сдвиг вправо на $k$ бит
- $\ll k$ — логический сдвиг влево на $k$ бит
- $\lor$ — побитовое ИЛИ (OR)

**Назначение функции mix:**

1. **Улучшение распределения:** Преобразует неравномерно распределённые байты ключа в более равномерное распределение выбираемых операций
   
2. **Усиление лавинного эффекта:** Изменение одного бита в байте ключа приводит к изменению нескольких битов в результате mix, что в свою очередь может привести к выбору совершенно другой операции
   
3. **Предотвращение слабых ключей:** Без функции mix некоторые значения ключа могли бы приводить к выбору одних и тех же или похожих операций, что ослабило бы шифр

#### 3.3.3 Выбор операции

На основе перемешанного байта ключа выбирается индекс операции:

$$j_i = \text{mix}(K[i]) \bmod 127$$

где:
- $K[i]$ — $i$-й байт ключа, $i \in \{0, 1, \ldots, m-1\}$
- $\bmod 127$ — остаток от деления на 127 (количество доступных операций)

**Процесс выбора:**
1. Извлекается байт ключа: $k = K[i]$
2. Байт перемешивается: $k' = \text{mix}(k)$
3. Вычисляется индекс: $j = k' \bmod 127$
4. Применяется операция: $O_j$

#### 3.3.4 Циклический побитовый сдвиг

После каждой операции куба Рубика применяется циклический сдвиг:

$$\rho_d : \{0,1\}^s \to \{0,1\}^s$$

$$\rho_d(X) = (X \ll d) \lor (X \gg (s - d))$$

где весь блок рассматривается как единая битовая последовательность длины $s$, которая циклически сдвигается влево на $d$ бит.

**Выбор параметра $d$ (в реализации):**

В этой реализации $d$ выбирается для каждого раунда по следующему правилу:
$$d_i = (\text{RK}_i[1] \bmod 8)$$
Если раундовые ключи не используются (WBC1), применяется простой детерминированный выбор
$$d_i = i \bmod 8$$
При отсутствии второго байта раундового ключа также используется $d_i = i \bmod 8$.

**Эффект циклического сдвига:**
- Обеспечивает **диффузию** — распространение изменений по всему блоку
- Предотвращает локализацию изменений в отдельных частях блока
- Усиливает взаимодействие между различными частями данных

### 3.4 Детальное описание процесса шифрования

**Алгоритм шифрования:**

$$
\begin{aligned}
&\textbf{Вход:} \text{ } B_b \in \{0,1\}^s \text{ (блок открытого текста), } K \in \{0,1\}^{k_{\text{key}}} \text{ (ключ)}\\
&\textbf{Выход:} \text{ } C_b \in \{0,1\}^s \text{ (зашифрованный блок)}\\
&\\
&1. \text{ } X^{(0)} = B_b \quad \text{// инициализация рабочего буфера}\\
&2. \text{ } \textbf{Для } i \text{ от } 0 \text{ до } m-1:\\
&\quad 2.1. \text{ } k = K[i] \quad \text{// извлечь i-й байт ключа}\\
&\quad 2.2. \text{ } k' = \text{mix}(k) \quad \text{// перемешать байт (ПЕРВЫМ!)}\\
&\quad 2.3. \text{ } j_i = k' \bmod 127 \quad \text{// вычислить индекс операции}\\
&\quad 2.4. \text{ } Y = O_{j_i}(X^{(i)}) \quad \text{// применить операцию куба Рубика}\\
&\quad 2.5. \text{ } X^{(i+1)} = \rho_{\alpha_i}(Y) \quad \text{// циклический сдвиг, } \alpha_i = i \bmod 8\\
&3. \text{ } C_b = X^{(m)} \quad \text{// результат — шифротекст блока}\\
&4. \text{ } \textbf{вернуть } C_b
\end{aligned}
$$

**Математическая запись одного раунда:**

Раунд $i$ преобразует блок $X$ следующим образом:

$$\text{Round}_i(X) = \rho_{\alpha_i}(O_{j_i}(X)), \quad j_i = \text{mix}(K[i]) \bmod 127$$

**Полная функция шифрования:**

$$E(B_b, K) = \text{Round}_{m-1} \circ \text{Round}_{m-2} \circ \cdots \circ \text{Round}_1 \circ \text{Round}_0(B_b)$$

или в развёрнутом виде:

$$E(B_b, K) = \rho_d(O_{j_{m-1}}(\rho_d(O_{j_{m-2}}(\cdots \rho_d(O_{j_1}(\rho_d(O_{j_0}(B_b)))) \cdots))))$$

где $j_i = \text{mix}(K[i]) \bmod 127$ для всех $i \in \{0, 1, \ldots, m-1\}$.

**Полная функция шифрования всего текста:**

$$E(T, K) = E(B_0, K) || E(B_1, K) || \cdots || E(B_{k-1}, K)$$

### 3.5 Процесс дешифрования

Дешифрование выполняется применением обратных операций в обратном порядке:

$$D(C_b, K) = \text{Round}_0^{-1} \circ \text{Round}_1^{-1} \circ \cdots \circ \text{Round}_{m-2}^{-1} \circ \text{Round}_{m-1}^{-1}(C_b)$$

**Обратный раунд:**

$$\text{Round}_i^{-1}(X) = O_j^{-1}(\rho_{-d}(X))$$

где:
- $рь\rho_{-d}(X) = (X \gg d) \lor (X \ll (s - d))$ — обратный циклический сдвиг
- $O_j^{-1}$ — обратная операция куба Рубика
- $j = \text{mix}(K[i]) \bmod 127$ — тот же индекс, что и при шифровании

**Алгоритм дешифрования:**

$$
\begin{aligned}
&\textbf{Вход:} \text{ } C_b \in \{0,1\}^s \text{ (шифротекст блока), } K \in \{0,1\}^{k_{\text{key}}} \text{ (ключ)}\\
&\textbf{Выход:} \text{ } T \in \{0,1\}^s \text{ (открытый текст блока)}\\
&\\
&1. \text{ } X^{(m)} = C_b\\
&2. \text{ } \textbf{Для } i \text{ от } m-1 \text{ до } 0 \text{ (в обратном порядке)}:\\
&\quad 2.1. \text{ } X^{(i)} = \rho_{-d}(X^{(i+1)}) \quad \text{// обратный циклический сдвиг}\\
&\quad 2.2. \text{ } k = K[i]\\
&\quad 2.3. \text{ } k' = \text{mix}(k) \quad \text{// то же перемешивание}\\
&\quad 2.4. \text{ } j = k' \bmod 127\\
&\quad 2.5. \text{ } X^{(i)} = O_j^{-1}(X^{(i)}) \quad \text{// обратная операция}\\
&3. \text{ } T = X^{(0)}\\
&4. \text{ } \textbf{вернуть } T
\end{aligned}
$$

---

## 4. Алгоритм WBC2 / WBC2 Algorithm

**Примечание:** далее описание алгоритма приводится для одного блока $B_b$; весь текст $T$ обрабатывается покомпонентно по блокам $B_b$.

### 4.1 Мотивация и улучшения

Алгоритм WBC2 представляет собой значительное усовершенствование WBC1, добавляя несколько криптографических слоёв для повышения стойкости к атакам.

**Основные улучшения:**

1. **Раундовые ключи:** Вместо прямого использования мастер-ключа, генерируются независимые раундовые ключи через криптографическую хеш-функцию SHA-256

2. **S-box (таблица замен):** Добавлена индивидуализированная S-box, зависящая от ключа, которая обеспечивает дополнительную нелинейность и конфьюжн

3. **Двухслойная диффузия:** Вместо простого циклического сдвига используется сложная двухслойная диффузия:
   - **Слой 1:** Структура Фейстеля для линейного перемешивания байтов
   - **Слой 2:** Самообратная перестановка для дополнительного распространения изменений

4. **Конфигурируемое количество раундов:** Количество раундов может настраиваться (по умолчанию 10), что позволяет балансировать между безопасностью и производительностью

### 4.2 Генерация раундовых ключей

Раундовые ключи генерируются из мастер-ключа с использованием криптографической хеш-функции:

$$\text{RK}_i = \text{SHA256}(K \, || \, i) \bmod 2^s$$

где:
- $K$ — мастер-ключ
- $i$ — номер раунда ($i \in \{0, 1, \ldots, r\}$)
- $||$ — операция конкатенации
- результат урезается до размера блока $s$ бит

**Свойства раундовых ключей:**
- **Независимость:** каждый раундовый ключ статистически независим от других
- **Детерминированность:** одинаковый мастер-ключ всегда даёт одинаковые раундовые ключи
- **Псевдослучайность:** раундовые ключи выглядят случайными благодаря свойствам SHA-256

### 4.3 Построение S-box

S-box строится индивидуально для каждого ключа с использованием алгоритма Fisher-Yates shuffle.

**Почему S-box работает с байтами ($\{0,1\}^8$):**

S-box в WBC2 спроектирован как **побайтовое преобразование**:

$$S : \{0,1\}^8 \to \{0,1\}^8$$

Это означает, что S-box принимает на вход один байт (8 бит = значение от 0 до 255) и выдаёт один байт.

**Причины использования байтового S-box:**

1. **Эффективность:** Современные процессоры оптимизированы для работы с байтами. Побайтовая обработка в 8 раз быстрее побитовой.

2. **Размер таблицы:** S-box может быть реализован как простой массив из 256 элементов, что умещается в кеш процессора:
   ```
   S[0] = значение для входа 0
   S[1] = значение для входа 1
   ...
   S[255] = значение для входа 255
   ```

3. **Криптографические свойства:** Байтовые S-box'ы используются в проверенных стандартах (AES, DES), так как 256-элементный S-box обеспечивает хороший компромисс между безопасностью и эффективностью.

4. **Биективность:** S-box должен быть биекцией (взаимно однозначным отображением), что обеспечивает обратимость. Для байтового S-box это означает, что каждое из 256 входных значений отображается в уникальное выходное значение.

**Алгоритм построения S-box:**

$$
\begin{aligned}
&1. \text{ } \textbf{Инициализация:} \text{ } S[i] = i \text{ для } i = 0, 1, \ldots, 255\\
&2. \text{ } \textbf{Для } i \text{ от } 255 \text{ до } 1:\\
&\quad 2.1. \text{ } \text{seed} = \text{SHA256}(K \, || \, \text{"sbox"} \, || \, i)\\
&\quad 2.2. \text{ } j = \text{seed} \bmod (i + 1)\\
&\quad 2.3. \text{ } \text{обменять } S[i] \leftrightarrow S[j]\\
&3. \text{ } \textbf{Построить обратную S-box:} \text{ } S^{-1}[S[i]] = i
\end{aligned}
$$

**Свойства полученного S-box:**
- **Биекция:** каждое значение от 0 до 255 появляется ровно один раз
- **Детерминированность:** одинаковый ключ всегда даёт одинаковый S-box
- **Ключевая зависимость:** разные ключи дают различные S-box'ы
- **Псевдослучайность:** S-box выглядит случайным, но воспроизводим

### 4.4 Двухслойная диффузия

Вместо простого циклического сдвига, WBC2 использует двухслойную диффузию.

#### 4.4.1 Слой 1: Структура Фейстеля

Первый слой использует структуру Фейстеля для линейного перемешивания байтов:

$$D_1 : \{0,1\}^s \to \{0,1\}^s$$

Блок разделяется на две половины: $X = (L, R)$ где $|L| = |R| = s/2$

**Три раунда Фейстеля:**

$$
\begin{aligned}
T_1 &= R \oplus F_1(L)\\
T_2 &= L \oplus F_2(T_1)\\
R' &= T_1 \oplus F_3(T_2)\\
L' &= T_2 \oplus F_1(R')
\end{aligned}
$$

где функция $F_k$ определяется как:

$$F_k(X) = \bigoplus_{i=0}^{|X|-1} (X[i] \oplus X[(i+k) \bmod |X|])$$

**Результат:** $D_1(X) = (L', R')$

**Обратное преобразование:**

$$
\begin{aligned}
T_2 &= L' \oplus F_1(R')\\
T_1 &= R' \oplus F_3(T_2)\\
L &= T_2 \oplus F_2(T_1)\\
R &= T_1 \oplus F_1(L)
\end{aligned}
$$

#### 4.4.2 Слой 2: Самообратная перестановка

Второй слой выполняет простую перестановку пар байтов:

$$D_2 : \{0,1\}^s \to \{0,1\}^s$$

**Операция:** обмен соседних пар байтов в каждой половине блока:

$$
\begin{aligned}
&\text{В первой половине:} \quad \text{swap}(X[i], X[i+1]) \text{ для } i = 0, 2, 4, \ldots, s/2-2\\
&\text{Во второй половине:} \quad \text{swap}(X[i], X[i+1]) \text{ для } i = s/2, s/2+2, \ldots, s-2
\end{aligned}
$$

**Свойство самообратимости:** $D_2(D_2(X)) = X$

**Полная диффузия:**

$$D(X) = D_2(D_1(X))$$

**Обратная диффузия:**

$$D^{-1}(X) = D_1^{-1}(D_2(X))$$

(так как $D_2$ самообратна: $D_2^{-1} = D_2$)

### 4.5 Структура раундов WBC2

Определим $W_K(B_b) = \text{Round}_{m-1} \circ \text{Round}_{m-2} \circ \cdots \circ \text{Round}_0(B_b)$ — преобразование WBC1 блока с мастер-ключом $K$, где

$$\text{Round}_i(B_b) = \rho_{\alpha_i}(O_{\text{mix}(K[i]) \bmod 127}(B_b))$$

**Раунд 0 (начальный):**

$$\text{Round}_0(B_b) = D(S(W_K(B_b) \oplus \text{RK}_0))$$

где:
1. $W_K(B_b)$ — применение последовательности операций WBC1 к блоку
1. $B_b \oplus \text{RK}_0$ — XOR с раундовым ключом
2. $S(\cdot)$ — применение S-box к каждому байту блока
3. $D(\cdot)$ — двухслойная диффузия

Примечание: Раунд 0 включает операции куба Рубика (как в WBC1) перед применением XOR, S-box и диффузии.

**Раунды 1..r (последующие):**

$$\text{Round}_i(X) = \rho_d(D(S(O_j(X) \oplus \text{RK}_i)))$$

где:
1. $O_j$ — операция куба Рубика, выбранная на основе $\text{RK}_i[0]$
2. $\cdot \oplus \text{RK}_i$ — XOR с раундовым ключом
3. $S(\cdot)$ — применение S-box
4. $D(\cdot)$ — двухслойная диффузия
5. $\rho_d(\cdot)$ — циклический побитовый сдвиг

**Выбор операции в раунде $i$:**

$$j = \text{mix}(\text{RK}_i[0]) \bmod 127$$

### 4.6 Функция шифрования WBC2

**Полная функция шифрования:**

$$E(B_b, K) = \text{Round}_r \circ \text{Round}_{r-1} \circ \cdots \circ \text{Round}_1 \circ \text{Round}_0(B_b)$$

**Алгоритм шифрования:**

$$
\begin{aligned}
&\textbf{Вход:} \text{ } B_b \in \{0,1\}^s, K \in \{0,1\}^{k_{\text{key}}}\\
&\textbf{Выход:} \text{ } C_b \in \{0,1\}^s\\
&\\
&1. \text{ } \text{Генерация раундовых ключей:}\\
&\quad \text{RK}_i = \text{SHA256}(K \, || \, i) \text{ для } i = 0, 1, \ldots, r\\
&\\
&2. \text{ } \text{Построение S-box:}\\
&\quad S = \text{BuildSBox}(K)\\
&\\
&3. \text{ } \text{Раунд 0 (с операциями WBC1):}\\
&\quad X = B_b\\
&\quad \textbf{Для каждого байта ключа } K[u], \ u = 0, 1, \ldots, m-1:\\
&\quad\quad \text{Применить операцию куба Рубика и сдвиг (как в WBC1)}\\
&\quad X = D(S(X \oplus \text{RK}_0))\\
&\\
&4. \text{ } \text{Раунды } 1..r:\\
&\quad \textbf{Для } i \text{ от } 1 \text{ до } r:\\
&\quad\quad j = \text{mix}(\text{RK}_i[0]) \bmod 127\\
&\quad\quad X = O_j(X)\\
&\quad\quad X = X \oplus \text{RK}_i\\
&\quad\quad X = S(X)\\
&\quad\quad X = D(X)\\
&\quad\quad X = \rho_d(X)\\
&\\
&5. \text{ } C_b = X\\
&6. \text{ } \textbf{вернуть } C_b
\end{aligned}
$$

### 4.7 Функция дешифрования WBC2

$$D(C_b, K) = \text{Round}_0^{-1} \circ \text{Round}_1^{-1} \circ \cdots \circ \text{Round}_{r-1}^{-1} \circ \text{Round}_r^{-1}(C_b)$$

**Обратный раунд $i$ (для $i \geq 1$):**

$$\text{Round}_i^{-1}(X) = O_j^{-1}(S^{-1}(D^{-1}(\rho_{-d}(X))) \oplus \text{RK}_i)$$

**Алгоритм дешифрования:**

$$
\begin{aligned}
&\textbf{Вход:} \text{ } C_b \in \{0,1\}^s, K \in \{0,1\}^{k_{\text{key}}}\\
&\textbf{Выход:} \text{ } T \in \{0,1\}^s\\
&\\
&1. \text{ } \text{Генерация тех же раундовых ключей и S-box}\\
&\\
&2. \text{ } X = C_b\\
&\\
&3. \text{ } \text{Обратные раунды } r..1:\\
&\quad \textbf{Для } i \text{ от } r \text{ до } 1 \text{ (в обратном порядке)}:\\
&\quad\quad X = \rho_{-d}(X)\\
&\quad\quad X = D^{-1}(X)\\
&\quad\quad X = S^{-1}(X)\\
&\quad\quad X = X \oplus \text{RK}_i\\
&\quad\quad j = \text{mix}(\text{RK}_i[0]) \bmod 127\\
&\quad\quad X = O_j^{-1}(X)\\
&\\
&4. \text{ } \text{Обратный раунд 0:}\\
&\quad X = W_K^{-1}(S^{-1}(D^{-1}(X)) \oplus \text{RK}_0)\\
&\\
&5. \text{ } T = X\\
&6. \text{ } \textbf{вернуть } T
\end{aligned}
$$

---

## 5. Сравнение алгоритмов / Algorithm Comparison

### 5.1 Сравнительная таблица

| Характеристика | WBC1 | WBC2 |
|----------------|------|------|
| **Основные операции** | Куб Рубика + сдвиг | Куб Рубика + XOR + S-box + диффузия |
| **Использование ключа** | Прямое (побайтово) | Через раундовые ключи (SHA-256) |
| **S-box** | Нет | Есть (индивидуализированный, $\{0,1\}^8 \to \{0,1\}^8$) |
| **Диффузия** | Циклический сдвиг | Двухслойная (Фейстель + перестановка) |
| **Количество раундов** | $m$ (длина ключа в байтах) | Конфигурируемое (по умолчанию 10) |
| **Операций на блок** | $m$ операций + $m$ сдвигов | Раунд 0 + $r$ раундов с полной обработкой |
| **Производительность** | ~4 сек/МБ | ~4.5 сек/МБ |
| **Чувствительность к ключу** | Хорошая (~45-50%) | Отличная (~48-52%) |
| **Лавинный эффект** | Хороший (~48-52%) | Отличный (~50-54%) |
| **Уровень безопасности** | Хороший | Повышенный |
| **Простота реализации** | Простая | Средняя |
| **Потребление памяти** | Низкое (~10 КБ) | Среднее (~20 КБ) |

### 5.2 Детальный анализ различий

**Криптографические улучшения в WBC2:**

1. **Раундовые ключи через SHA-256:**
   - WBC1 использует байты ключа напрямую
   - WBC2 генерирует независимые раундовые ключи через криптографическую хеш-функцию
   - Это предотвращает атаки, основанные на связях между байтами ключа

2. **Индивидуализированный S-box:**
   - WBC1 не имеет S-box
   - WBC2 имеет ключезависимый S-box ($S : \{0,1\}^8 \to \{0,1\}^8$)
   - S-box добавляет существенную нелинейность и конфьюжн
   - Каждый ключ порождает уникальный S-box

3. **Усиленная диффузия:**
   - WBC1: простой циклический сдвиг
   - WBC2: двухслойная диффузия (Фейстель + перестановка)
   - Обеспечивает лучшее распространение изменений по блоку

4. **XOR с раундовыми ключами:**
   - WBC2 добавляет XOR на каждом раунде
   - Усиливает зависимость от ключа
   - Добавляет дополнительный слой защиты

**Компромиссы производительности:**

- WBC2 примерно на 12% медленнее WBC1
- Дополнительное время тратится на:
  - Генерацию раундовых ключей (разовая операция)
  - Построение S-box (разовая операция)
  - Применение S-box к каждому байту
  - Более сложную диффузию
- Для большинства применений ~12% замедление — приемлемая цена за повышенную безопасность

### 5.3 Анализ сложности и эффективности

**Обозначения:**
- $s$ — размер блока в битах, $b = s/8$ — размер блока в байтах
- $n$ — длина текста в битах, $k = \lceil n/s \rceil$ — число блоков
- $m$ — число байтов ключа (WBC1)
- $r$ — число раундов (WBC2)

**WBC1 (оригинальное описание):**
- На каждом шаге выполняются перестановка $O_j$ и циклический сдвиг $\rho_d$, обе операции линейны по размеру блока.
- Сложность одного блока: $$T_{\text{WBC1}}(\text{block}) = O(m \cdot s)$$
- Сложность всего текста: $$T_{\text{WBC1}}(\text{text}) = O(k \cdot m \cdot s) \approx O(n \cdot m)$$
- Эффективность: высокая, так как используются только перестановки и сдвиги; стоимость на байт постоянна.

**WBC2:**
- Каждый раунд включает: перестановку $O_j$, XOR с раундовым ключом, S-box, диффузию и сдвиг.
- Сложность одного блока: $$T_{\text{WBC2}}(\text{block}) = O(r \cdot s)$$
- Сложность всего текста: $$T_{\text{WBC2}}(\text{text}) = O(k \cdot r \cdot s) \approx O(n \cdot r)$$
- Предобработка (однократно): генерация раундовых ключей $O(r \cdot s)$ и S-box $O(256)$.
- Эффективность: ниже, чем у WBC1, из-за S-box и усиленной диффузии, но остается линейной по размеру блока.

---

## 6. Криптографические свойства / Cryptographic Properties

### 6.1 Диффузия (Diffusion)

**Определение:** Свойство, при котором изменение одного бита входа влияет на многие биты выхода.

**WBC1:**
- Диффузия обеспечивается циклическими сдвигами
- После $m$ раундов изменения распространяются по всему блоку
- Качество: хорошее (~48-52% изменённых бит)

**WBC2:**
- Усиленная диффузия через двухслойную структуру:
  - Фейстелева сеть (байт-уровень)
  - Перестановка пар (локальная диффузия)
  - Циклический сдвиг (глобальная диффузия)
- Качество: отличное (~50-54% изменённых бит)

### 6.2 Конфьюжн (Confusion)

**Определение:** Сложность связи между ключом и шифротекстом.

**WBC1:**
- Конфьюжн через операции куба Рубика
- Функция mix улучшает связь ключ-операция
- Нелинейность через комбинацию операций

**WBC2:**
- Дополнительная конфьюжн через S-box ($S : \{0,1\}^8 \to \{0,1\}^8$)
- Раундовые ключи из SHA-256 скрывают мастер-ключ
- Многослойная нелинейность

### 6.3 Чувствительность к ключу (Key Sensitivity)

**Определение:** Изменение одного бита ключа должно существенно изменять шифротекст.

**WBC1:**
- Функция mix обеспечивает чувствительность
- Изменение бита ключа может изменить выбор операции
- Измеренная чувствительность: ~45-50%

**WBC2:**
- SHA-256 для раундовых ключей усиливает чувствительность
- Ключезависимый S-box добавляет глобальную зависимость
- Измеренная чувствительность: ~48-52%

### 6.4 Лавинный эффект (Avalanche Effect)

**Определение:** Изменение одного бита входа должно изменять примерно 50% битов выхода.

**Математическое выражение:**

$$\text{Avalanche} = \frac{1}{s} \sum_{i=1}^{s} |E(T, K)[i] \oplus E(T \oplus e_j, K)[i]|$$

где $e_j$ — битовый вектор с единицей только в позиции $j$.

**Результаты:**
- WBC1: 48-52% (хороший лавинный эффект)
- WBC2: 50-54% (отличный лавинный эффект, близко к идеальному)

### 6.5 Стойкость к атакам

**Линейный криптоанализ:**
- Нелинейность операций куба Рубика затрудняет линейные аппроксимации
- S-box в WBC2 добавляет дополнительную нелинейность

**Дифференциальный криптоанализ:**
- Функция mix и операции куба создают сложные дифференциальные характеристики
- Двухслойная диффузия в WBC2 усложняет отслеживание разностей

**Атаки связанных ключей:**
- Раундовые ключи в WBC2 (через SHA-256) защищают от таких атак
- WBC1 более уязвим к связанным ключам

---

## 7. Заключение / Conclusion

### 7.1 Основные результаты

Представлены два алгоритма блочного шифрования:

**WBC1** — эффективный алгоритм, основанный на операциях куба Рубика:
- Простая и эффективная реализация
- Хорошие криптографические свойства
- Подходит для общих задач шифрования

**WBC2** — усовершенствованная версия с дополнительными криптографическими слоями:
- Раундовые ключи через SHA-256
- Индивидуализированный S-box ($S : \{0,1\}^8 \to \{0,1\}^8$, работающий с байтами)
- Двухслойная диффузия
- Повышенная стойкость к атакам

### 7.2 Рекомендации по использованию

**Используйте WBC1 когда:**
- Требуется высокая скорость обработки
- Обрабатываются большие объёмы данных
- Ограничены ресурсы памяти
- Требуется простота реализации

**Используйте WBC2 когда:**
- Безопасность критична
- Обрабатываются чувствительные данные
- Доступны дополнительные ресурсы
- Требуется максимальная стойкость

### 7.3 Объяснение ключевых нотаций

**Почему $\{0,1\}^8$ для S-box:**

Нотация $S : \{0,1\}^8 \to \{0,1\}^8$ используется потому, что:
1. S-box работает с **байтами** (8-битными значениями)
2. $\{0,1\}^8$ — математически точный способ обозначить "множество всех 8-битных последовательностей"
3. Это эквивалентно множеству $\{0, 1, 2, \ldots, 255\}$ (256 значений)
4. Стандартная практика в криптографии (используется в AES, DES и других)

**Преимущества побайтовой обработки:**
- Эффективность (в 8 раз быстрее побитовой)
- Простота реализации
- Оптимальный размер таблицы (256 элементов)
- Совместимость с процессорной архитектурой

### 7.4 Направления будущих исследований

1. **Формальные доказательства безопасности**
2. **Оптимизация с использованием SIMD/GPU**
3. **Защита от атак по сторонним каналам**
4. **Квантовая стойкость**

---

## 8. Параллельная обработка / Parallel Processing

### 8.1 Мотивация

Одной из ключевых особенностей реализации алгоритмов WBC1 и WBC2 является поддержка **параллельной обработки** с использованием технологии **MPI (Message Passing Interface)**.

Блочные шифры, такие как WBC1 и WBC2, обладают важным свойством: каждый блок может быть зашифрован **независимо** от других блоков. Это делает их идеально подходящими для параллелизации.

### 8.2 Предварительные настройки / Preliminary Settings

**Після попередніх налаштувань** (After preliminary settings), система выполняет следующие шаги инициализации:

1. **Инициализация шифра:**
   $$\text{Init}(K, s, r) \to \text{CipherState}$$

2. **Настройка ключа:**
   - Для WBC1: определение операций на основе байтов ключа
   - Для WBC2: генерация раундовых ключей через SHA-256

3. **Генерация S-box (WBC2):**
   $$S : \{0,1\}^8 \to \{0,1\}^8$$
   Используя Fisher-Yates shuffle с ключом

4. **Генерация раундовых ключей (WBC2):**
   $$\text{RK}_i = \text{SHA256}(K || i), \quad i = 0, 1, \ldots, r$$

5. **Инициализация таблицы операций:**
   Загрузка 127 операций куба Рубика

**Важно:** Эти настройки выполняются **один раз** перед началом шифрования всех блоков.

### 8.3 Последовательность этапов шифрования / Sequential Encryption Stages

**Етапи шифрування виконуються в чіткій послідовності** (Encryption stages are performed in strict sequence).

Для каждого блока $B_b$ выполняется одна и та же последовательность операций:

$$C_b = E(B_b, K)$$

**Для WBC1:**
$$C_b = \text{Round}_{m-1} \circ \text{Round}_{m-2} \circ \cdots \circ \text{Round}_1 \circ \text{Round}_0(B_b)$$

**Для WBC2:**
$$C_b = \text{Round}_r \circ \text{Round}_{r-1} \circ \cdots \circ \text{Round}_1 \circ \text{Round}_0(B_b)$$

**Важно:** Внутри каждого блока операции выполняются **строго последовательно**, но разные блоки шифруются **независимо и параллельно**.

### 8.4 Распределение блоков между процессорами / Block Distribution Among Processors

**Кожен блок розподіляється між процесорами** (Each block is distributed among processors).

#### 7.4.1 Механизм распределения / Distribution Mechanism

После разбиения текста на блоки $B_0, B_1, \ldots, B_{k-1}$, блоки **равномерно распределяются** между процессами.

В реализации `wbc2_original_parallel.c` используется схема с `MPI_Scatterv/MPI_Gatherv`: каждому процессу передаётся **непрерывный сегмент** массива блоков, а число блоков на процессе задаётся через `sendcounts` и `displs`.

Обозначим:

$$\text{base}=\left\lfloor\frac{k}{P}\right\rfloor,\qquad \text{extra}=k\bmod P$$

Тогда число блоков у процесса $p$:

$$n_p=\text{base}+\begin{cases}
1, & p<\text{extra}\\
0, & p\ge \text{extra}
\end{cases}$$

Следовательно,

$$n_p\in\left\{\left\lfloor\frac{k}{P}\right\rfloor,\left\lceil\frac{k}{P}\right\rceil\right\}.$$

#### 7.4.2 Количество блоков на процесс / Blocks Per Process

Каждый процесс получает примерно $k/P$ блоков:

$$\text{Блоков на процесс} \approx \lceil k/P \rceil$$

**Точное распределение:**

- Если $k \bmod P = 0$: все процессы получают ровно $k/P$ блоков
- Если $k \bmod P \neq 0$: первые $(k \bmod P)$ процессов получают $\lceil k/P \rceil$ блоков, остальные получают $\lfloor k/P \rfloor$ блоков

**Формула для процесса $p$:**

$$\text{count}_p = \begin{cases}
\lceil k/P \rceil & \text{если } p < (k \bmod P) \\
\lfloor k/P \rfloor & \text{если } p \geq (k \bmod P)
\end{cases}$$

#### 7.4.3 Пример распределения / Distribution Example

**Пример 1: $k = 16$ блоков, $P = 4$ процесса (равномерное)**

$k \bmod P = 16 \bmod 4 = 0$.

| Процессор | Непрерывный диапазон блоков | Количество |
|-----------|-----------------------------|------------|
| Process 0 | $B_0\ldots B_3$            | 4 блока    |
| Process 1 | $B_4\ldots B_7$            | 4 блока    |
| Process 2 | $B_8\ldots B_{11}$         | 4 блока    |
| Process 3 | $B_{12}\ldots B_{15}$      | 4 блока    |

**Пример 2: $k = 10$ блоков, $P = 4$ процесса (неравномерное)**

$k \bmod P = 10 \bmod 4 = 2$ — первые 2 процесса получают по 3 блока.

| Процессор | Непрерывный диапазон блоков | Количество |
|-----------|-----------------------------|------------|
| Process 0 | $B_0\ldots B_2$            | 3 блока    |
| Process 1 | $B_3\ldots B_5$            | 3 блока    |
| Process 2 | $B_6\ldots B_7$            | 2 блока    |
| Process 3 | $B_8\ldots B_9$            | 2 блока    |

**Пример 3: $k = 1000$ блоков, $P = 8$ процессов**

$k \bmod P = 1000 \bmod 8 = 0$.

- Каждый процесс обрабатывает $1000/8 = 125$ блоков.
- Process 0: $B_0\ldots B_{124}$.
- Process 1: $B_{125}\ldots B_{249}$.
- ...
- Process 7: $B_{875}\ldots B_{999}$.

#### 7.4.4 Визуализация распределения / Distribution Visualization

**$k = 16$ блоков, $P = 4$ процесса (непрерывные сегменты):**
```
Process 0: ████ ████ ████ ████
Process 1:                     ████ ████ ████ ████
Process 2:                                         ████ ████ ████ ████
Process 3:                                                             ████ ████ ████ ████
           B₀  B₁  B₂  B₃  B₄  B₅  B₆  B₇  B₈  B₉  B₁₀ B₁₁ B₁₂ B₁₃ B₁₄ B₁₅
```

**$k = 10$ блоков, $P = 4$ процесса:**
```
Process 0: ████ ████ ████
Process 1:                 ████ ████ ████
Process 2:                                 ████ ████
Process 3:                                         ████ ████
           B₀  B₁  B₂  B₃  B₄  B₅  B₆  B₇  B₈  B₉
```

#### 7.4.5 Роль Root процесса / Role of Root Process

**Root процесс (rank 0)** выполняет дополнительные задачи:

1. **Чтение входных данных:**
   - Читает файл с открытым текстом
   - Разбивает текст на блоки $B_0, B_1, \ldots, B_{k-1}$

2. **Распределение блоков:**
   - Использует `MPI_Scatterv` для распределения блоков между всеми процессами
   - Себе также получает собственный непрерывный сегмент блоков

3. **Сбор результатов:**
   - Использует `MPI_Gatherv` для сбора зашифрованных блоков от всех процессов
   - Собирает результаты в правильном порядке

4. **Запись результатов:**
   - Записывает итоговый шифртекст $C = C_0 || C_1 || \cdots || C_{k-1}$ в файл

**Остальные процессы (rank > 0):**
- Получают свою долю блоков
- Шифруют блоки независимо
- Отправляют результаты обратно root процессу

#### 7.4.6 Детали MPI распределения / MPI Distribution Details

**Использование MPI_Scatterv:**

```c
// Root процесс имеет все блоки
if (rank == 0) {
   // all_blocks содержит B₀, B₁, ..., B_{k-1}
}

// Вычисление размера chunk для каждого процесса
int base_count = k / P;          // Базовое количество блоков
int remainder = k % P;            // Остаток
int my_count = base_count + (rank < remainder ? 1 : 0);

// Распределение блоков с переменными размерами
MPI_Scatterv(all_blocks,     // Буфер отправки (только на root)
             sendcounts,     // Размер данных для каждого процесса
             displs,         // Смещения
             MPI_BYTE,       // Тип данных
             local_blocks,   // Буфер приёма (на всех процессах)
             my_count_bytes, // Размер приёма
             MPI_BYTE,       // Тип данных
             0,              // Root процесс (rank 0)
             MPI_COMM_WORLD);// Коммуникатор
```

**Важно:** 
- `MPI_Scatterv` и `MPI_Gatherv` корректно обрабатывают случай, когда $k \bmod P \neq 0$.
- Каждый процесс получает непрерывный фрагмент массива блоков.
- Порядок блоков восстанавливается на root-процессе через `displs`.

#### 7.4.7 Преимущества блочно-непрерывного распределения / Benefits of Contiguous Block Distribution

1. **Равномерная балансировка нагрузки:**
   - Разница между процессами максимум 1 блок
   - Минимизация простоя процессоров

2. **Простота реализации:**
   - Достаточно вычислить массивы `sendcounts` и `displs`
   - Не требует сложных структур данных

3. **Устойчивость к вариациям:**
   - Если некоторые блоки обрабатываются быстрее, нагрузка всё равно равномерна
   - Хорошо работает для блоков одинакового размера

4. **Оптимальное использование кэша:**
   - Процессы обрабатывают непрерывные диапазоны блоков
   - Улучшенная пространственная локальность данных

### 8.5 Параллелизация алгоритма / Algorithm Parallelization

**Що дає можливість розпаралелити алгоритм** (Which makes it possible to parallelize the algorithm).

**Преимущества:**

1. **Почти линейное ускорение (в идеальном случае):**
   $$\text{Speedup} = \frac{T_{\text{sequential}}}{T_{\text{parallel}}} \lesssim P$$

2. **Независимая обработка:**
   - Нет коммуникации между процессорами во время шифрования
   - Каждый процессор работает автономно

3. **Масштабируемость:**
   - Работает с любым числом процессоров
   - Эффективность близка к 100%

4. **Эффективность:**
   $$\text{Efficiency} = \frac{\text{Speedup}}{P} \times 100\%$$
   На практике снижается из-за коммуникаций (`Scatterv/Gatherv`), синхронизации и накладных расходов MPI.

### 8.6 MPI реализация / MPI Implementation

**Рабочий процесс:**

1. **Инициализация MPI:**
   ```c
   MPI_Init(&argc, &argv)
   MPI_Comm_rank(MPI_COMM_WORLD, &rank)
   MPI_Comm_size(MPI_COMM_WORLD, &size)
   ```

2. **Предварительные настройки (на всех процессах):**
   ```c
   wbc_init(&cipher, key, key_len, block_size, num_rounds)
   ```

3. **Распределение блоков (MPI_Scatterv):**
   ```c
   MPI_Scatterv(all_blocks, sendcounts, displs, MPI_BYTE,
                local_blocks, my_count_bytes, MPI_BYTE,
                0, MPI_COMM_WORLD)
   ```

4. **Параллельное шифрование:**
   ```c
   for (i = 0; i < local_block_count; i++) {
       wbc_encrypt_block(&cipher, local_blocks[i], local_ciphertexts[i])
   }
   ```

5. **Сбор результатов (MPI_Gatherv):**
   ```c
   MPI_Gatherv(local_ciphertexts, my_count_bytes, MPI_BYTE,
              all_ciphertexts, recvcounts, displs, MPI_BYTE,
              0, MPI_COMM_WORLD)
   ```

### 8.7 Характеристики производительности / Performance Characteristics

**Временная сложность:**

- **Последовательная:** $O(k \cdot C)$
- **Параллельная:** $O((k/P) \cdot C)$

где:
- $k$ — количество блоков
- $C$ — сложность шифрования одного блока
- $P$ — количество процессоров

**Пример производительности:**

| Блоков | Процессоров | Время | Ускорение | Эффективность |
|--------|-------------|-------|-----------|---------------|
| 1000   | 1           | 40.0s | 1.0×      | 100%          |
| 1000   | 2           | 20.1s | 1.99×     | 99.5%         |
| 1000   | 4           | 10.2s | 3.92×     | 98%           |
| 1000   | 8           | 5.3s  | 7.55×     | 94%           |

### 8.8 Практические соображения / Practical Considerations

**Оптимальное число процессоров:**

$$P_{\text{optimal}} = \min(k, P_{\text{available}})$$

Больше процессоров, чем блоков, не дает преимущества.

**Балансировка нагрузки:**

Когда $k \bmod P \neq 0$, некоторые процессы получают на один блок больше.

**Требования к памяти:**

Каждый процесс хранит:
- Состояние шифра: ~10 KB
- Локальные блоки: $(k/P) \times s$ бит
- Локальные шифртексты: $(k/P) \times s$ бит

### 8.9 Уточнённая оценка сложности для `wbc2_original_parallel` / Refined Complexity for `wbc2_original_parallel`

Ниже приведена оценка, согласованная с текущей C-реализацией `wbc2_original_parallel.c`.

Пусть:
- $n$ — длина входных данных в битах;
- $s$ — размер блока в битах;
- $k=\lceil n/s \rceil$ — число блоков;
- $l$ — длина мастер-ключа в битах;
- $m=l/8$ — длина ключа в байтах;
- $R$ — число раундов WBC2 (параметр `num_rounds`);
- $P$ — число MPI-процессов.

**Сложность одного блока.**

Раунд 0:

$$m\cdot\big(O_{O_j}(s)+O_{\rho}(s)\big)+O_{\oplus}(s)+O_S(s)+O_D(s)$$

Раунды $1,\ldots,R$:

$$R\cdot\big(O_{O_j}(s)+O_{\oplus}(s)+O_S(s)+O_D(s)+O_{\rho}(s)\big)$$

Так как все перечисленные операции линейны по $s$, получаем:

$$T_{\text{block}}=O\big((m+R)\cdot s\big).$$

**Одноразовые затраты инициализации.**

В данной реализации выполняются:
- генерация раундовых ключей: $(R+1)$ вызовов SHA-256;
- построение S-box: 1 вызов SHA-256 + Fisher-Yates/MT-перестановка $O(256)$;
- инициализация таблицы операций: 20 + 127 вызовов SHA-256.

Итого SHA-затраты инициализации:

$$C_{\text{init}}^{\text{SHA}}=(R+149)\cdot C_{\text{SHA}}.$$

**Полная последовательная сложность.**

$$T_1=O\big((R+149)\cdot C_{\text{SHA}}\big)+O\big(k\cdot(m+R)\cdot s\big).$$

Для больших $n$ доминирует линейная часть:

$$T_1=O\big(n\cdot(m+R)\big).$$

**Сложность дешифрования.**

Дешифрование содержит те же классы операций в обратном порядке, поэтому асимптотически:

$$T_{\text{dec}}=O\big(n\cdot(m+R)\big).$$

**Параллельная оценка (MPI).**

Каждый процесс обрабатывает

$$n_p\in\left\{\left\lfloor\frac{k}{P}\right\rfloor,\left\lceil\frac{k}{P}\right\rceil\right\}$$

блоков, и вычислительная часть времени оценивается как

$$O\!\left(\left\lceil\frac{k}{P}\right\rceil\cdot(m+R)\cdot s\right).$$

С учётом коммуникаций и синхронизации:

$$T_P=O\!\left(\frac{n}{P}\cdot(m+R)\right)+O_{\text{comm}}+O_{\text{sync}}.$$

Если использовать параметризацию через латентность/пропускную способность MPI, можно записать:

$$T_P=O\!\left(\frac{n}{P}\cdot(m+R)\right)+O(\tau_c\,t\,\log P)+O(\tau_o\,t\,n).$$

Тогда

$$S_P=\frac{T_1}{T_P},\qquad E_P=\frac{S_P}{P},$$

и в пределе больших $n$ при умеренных коммуникационных накладных расходах $S_P$ стремится к почти линейному росту по $P$.

### 8.10 PWBC2.1 для `wbc1_parallel_cached`: уточнённая формализация / Refined PWBC2.1 for `wbc1_parallel_cached`

Ниже приведена формализация, согласованная с реализацией `wbc1_parallel_cached.c`.

Пусть:
- $X^{(r)}$ — вход $r$-го раунда, $r=0,1,\ldots,R-1$;
- $X^{(0)}=B_i$ — входной блок;
- $RK_r$ — раундовый ключ;
- $\mathcal{P}=\{\pi_0,\ldots,\pi_{126}\}$ — таблица из 127 перестановок;
- $S$ — ключезависимая S-box;
- $P$ — число MPI-процессов.

**Важно для данной реализации:** размер блока фиксирован как $16$ байт ($128$ бит), то есть `BLOCK_SIZE=16`. Поэтому практическая реализация не использует правило $s=d^3$ байт.

#### 7.10.1 Параллельная обработка блоков

Входной текст разбивается на блоки $B_i$ длины $s$ бит (в реализации $s=128$).

Блоки распределяются между процессами через `MPI_Scatterv`:

$$n_p\in\left\{\left\lfloor\frac{k}{P}\right\rfloor,\left\lceil\frac{k}{P}\right\rceil\right\},\qquad k=\left\lceil\frac{n}{s}\right\rceil.$$

Каждый процесс независимо шифрует свой пакет блоков; сбор результата выполняется через `MPI_Gatherv`.

#### 7.10.2 Раунд шифрования (MODE\_FULL)

Для каждого раунда $r$ над блоком выполняется:

1. **Одна операция из таблицы перестановок**

$$j_r = RK_r[0] \bmod 127, \qquad X_1 = \pi_{j_r}(X^{(r)}).$$

2. **XOR с раундовым ключом**

$$X_2 = X_1 \oplus RK_r.$$

3. **S-box**

$$X_3[t] = S(X_2[t]),\quad t=0,1,\ldots,s_B-1,$$

где $s_B=s/8$ — размер блока в байтах.

4. **Кумулятивная XOR-диффузия (прямой проход)**

$$Y_0 = X_3[0], \qquad Y_t = X_3[t] \oplus Y_{t-1},\ t=1,\ldots,s_B-1.$$

Обозначим $X_4=(Y_0,\ldots,Y_{s_B-1})$.

5. **Циклический побитовый сдвиг в байтах блока**

$$\delta_r = RK_r[1] \bmod 8, \qquad X^{(r+1)}[t] = \operatorname{ROTR}_8(X_4[t], \delta_r).$$

Таким образом, в этой версии действительно выполняется **одна** операция $\pi_{j_r}$ из таблицы на раунд (а не серия операций внутри раунда).

#### 7.10.3 MODE\_SIMPLIFIED

В упрощённом режиме выполняются только:

$$X_1=\pi_{j_r}(X^{(r)}),\qquad X^{(r+1)}=\operatorname{ROTR}_8(X_1,\delta_r),$$

без шагов XOR, S-box и диффузии.

#### 7.10.4 Сбор итогового шифротекста

После завершения $R$ раундов для всех блоков:

$$C=(C_0,C_1,\ldots,C_{k-1})$$

восстанавливается на root-процессе в исходном порядке.

#### 7.10.5 PWBC2.1 у стилі WBC0: повна форма / PWBC2.1 in WBC0-Style: Full Form

$$
\begin{aligned}
&\textbf{Вхід:}\ B_t\in\{0,1\}^{s}\ \text{(блок відкритого тексту)},\ K\in\{0,1\}^{l}\ \text{(майстер-ключ)}\\
&\phantom{\textbf{Вхід:}}\ m=l/8\ \text{(довжина ключа в байтах)}\\
&\textbf{Вихід:}\ C_t\in\{0,1\}^{s}\ \text{(зашифрований блок)}\\
&1.\ RK^{(0)}\leftarrow K\\
&2.\ B_t\leftarrow U_0\,||\,U_1\,||\,\cdots\,||\,U_{q-1}\\
&3.\ \textbf{Для } r=0,1,\ldots,q-1:\\
&\quad 3.1\ X_t^{(0)}\leftarrow U_r\\
&\quad 3.2\ \textbf{Для } i=0,1,\ldots,m-1:\\
&\qquad z_i\leftarrow RK^{(r)}[i]\oplus \operatorname{ROTL}_8\!\big(RK^{(r)}[(i+1)\bmod m],\ i\bmod 8\big)\\
&\qquad\phantom{z_i\leftarrow}\oplus\operatorname{ROTR}_8\!\big(RK^{(r)}[(i-1+m)\bmod m],\ (i+3)\bmod 8\big)\oplus(29i\bmod 256)\\
&\qquad m_i\leftarrow \operatorname{mix}(z_i)\\
&\qquad j_i\leftarrow m_i\bmod 127\\
&\qquad Y_t^{(i)}\leftarrow O_{j_i}\!\big(X_t^{(i)}\big)\\
&\qquad \alpha_i\leftarrow i\bmod 8\\
&\qquad \beta_i\leftarrow m_i\bmod 4\\
&\qquad \delta_i\leftarrow (\alpha_i+\beta_i)\bmod(8u_r)\\
&\qquad X_t^{(i+1)}\leftarrow \rho_{\delta_i}\!\big(Y_t^{(i)}\big)\\
&\quad 3.3\ Z_r\leftarrow X_t^{(m)}\\
&\quad 3.4\ RK^{(r+1)}[\tau]\leftarrow Z_r[\tau\bmod(8u_r)],\ \tau=0,1,\ldots,l-1\\
&4.\ C_t\leftarrow Z_0\,||\,Z_1\,||\,\cdots\,||\,Z_{q-1}\\
&5.\ \textbf{Повернути } C_t
\end{aligned}
$$

#### 7.10.6 Аналіз складності PWBC2.1 / PWBC2.1 Complexity Analysis

Нехай:
- $n$ — довжина вхідних даних (відкритого тексту або шифротексту) у бітах;
- $s$ — розмір блока у бітах, $s_B=s/8$ — у байтах;
- $k=\lceil n/s\rceil$ — кількість блоків;
- $l$ — довжина майстер-ключа у бітах, $m=l/8$ — у байтах;
- $R$ — кількість раундів;
- $P_{\text{mpi}}$ — кількість MPI-процесів.

У теоретичній моделі блок може розглядатися як куб $d\times d\times d$, де $d=\sqrt[3]{s}$ (за потреби застосовується padding до $d^3$ біт), але для оцінки нижче асимптотика виводиться за фактичним розміром блока $s$.

**1) Одноразова ініціалізація (для фіксованого ключа $K$).**

- Побудова $S$ та $S^{-1}$: $\Theta(256)$ часу і $\Theta(256)$ пам'яті.
- Генерація раундових ключів $RK_r=\mathrm{SHA256}(K||r)_{s_B}$, $r=0,\ldots,R-1$:

$$T_{\text{RK}}=\Theta\!\left(R\cdot C_{\mathrm{SHA}}\right),$$

де $C_{\mathrm{SHA}}$ — вартість одного обчислення SHA-256 (для фіксованого $l$ — константа).

**2) Складність одного раунду шифрування блока.**

Для одного раунду:
- $X_1=O_{j_r}(X^{(r)})$ — лінійно за розміром блока: $\Theta(s_B)$;
- $X_2=X_1\oplus RK_r$ — $\Theta(s_B)$;
- $X_3[u]=S(X_2[u])$ для всіх байтів — $\Theta(s_B)$;
- префіксна XOR-дифузія $Y_u$ — $\Theta(s_B)$;
- побайтовий $\mathrm{ROTR}_8$ — $\Theta(s_B)$.

Отже,

$$T_{\text{round}}=\Theta(s_B)=\Theta(s).$$

**3) Складність шифрування одного блока.**

За $R$ раундів:

$$T_{\text{block}}=\Theta(R\cdot s_B)=\Theta(R\cdot s).$$

Якщо враховувати одноразову ініціалізацію для першого блока:

$$T_{\text{1st block}}=\Theta(256)+\Theta(R\cdot C_{\mathrm{SHA}})+\Theta(R\cdot s_B).$$

**4) Складність для $k$ блоків (послідовно).**

$$T_{1}(k)=\Theta(256)+\Theta(R\cdot C_{\mathrm{SHA}})+\Theta(k\cdot R\cdot s_B).$$

Для великих $n$ домінує:

$$T_{1}(k)=\Theta(k\cdot R\cdot s)=\Theta(n\cdot R).$$

**5) Паралельна оцінка (MPI).**

Кожен процес обробляє

$$n_p\in\left\{\left\lfloor\frac{k}{P_{\text{mpi}}}\right\rfloor,\left\lceil\frac{k}{P_{\text{mpi}}}\right\rceil\right\}$$

блоків, отже обчислювальна частина часу:

$$T_{P_{\text{mpi}}}^{\text{comp}}=\Theta\!\left(\left\lceil\frac{k}{P_{\text{mpi}}}\right\rceil\cdot R\cdot s_B\right).$$

З урахуванням накладних витрат на комунікації та синхронізацію:

$$T_{P_{\text{mpi}}}=\Theta\!\left(\left\lceil\frac{k}{P_{\text{mpi}}}\right\rceil\cdot R\cdot s_B\right)+O(\tau_c\,t\,\log P_{\text{mpi}})+O(\tau_o\,t\,n),$$

де останні два доданки відповідають латентності/пропускній здатності MPI-моделі.

За умови великих $n$ та помірних комунікаційних витрат:

$$T_{P_{\text{mpi}}}=\Theta\!\left(\frac{k\cdot R\cdot s}{P_{\text{mpi}}}\right).$$

Прискорення та ефективність:

$$S_{P_{\text{mpi}}}=\frac{T_1}{T_{P_{\text{mpi}}}},\qquad E_{P_{\text{mpi}}}=\frac{S_{P_{\text{mpi}}}}{P_{\text{mpi}}}.$$

**6) Просторова складність.**

- таблиці $S,S^{-1}$: $\Theta(256)$ байт;
- раундові ключі (якщо кешуються всі): $\Theta(R\cdot s_B)$ байт;
- робочі буфери раунду: $\Theta(s_B)$ байт.

Загалом:

$$M=\Theta(256)+\Theta(R\cdot s_B)+\Theta(s_B)=\Theta(R\cdot s_B+256).$$

Якщо ключі генеруються «на льоту» (без кешу всіх $RK_r$), тоді пам'ять зменшується до

$$M_{\text{stream}}=\Theta(s_B+256).$$

**7) Оцінка складності та ефективності CUDA-версії (PWBC2.1).**

Нехай:
- $G$ — кількість GPU-пристроїв;
- $B_{\text{gpu}}$ — ефективна пропускна здатність GPU-пам'яті/ядра (біт/с);
- $\tau_{H2D},\tau_{D2H}$ — вартість передач host→device та device→host на 1 біт;
- $T_{\text{launch}}$ — накладні витрати запуску CUDA-ядра.

Для одного блока обчислювальна частина на GPU зберігає ту саму асимптотику:

$$T_{\text{block}}^{\text{GPU,comp}}=\Theta(R\cdot s).$$

Для $k$ блоків при розподілі між $G$ GPU:

$$T_{\text{GPU,comp}}=\Theta\!\left(\frac{k\cdot R\cdot s}{G}\right).$$

З урахуванням передач і запусків:

$$T_{\text{GPU,total}}=\Theta\!\left(\frac{k\cdot R\cdot s}{G}\right)+O(k\cdot s\cdot\tau_{H2D})+O(k\cdot s\cdot\tau_{D2H})+O(T_{\text{launch}}).$$

Якщо використовується гібрид MPI+CUDA (по одному GPU на процес або відображення $g(p)=p\bmod G$), тоді:

$$T_{P_{\text{mpi}},G}\approx \Theta\!\left(\frac{k\cdot R\cdot s}{P_{\text{mpi}}\cdot G_{\text{eff}}}\right)+O(\tau_c\,t\,\log P_{\text{mpi}})+O(\tau_o\,t\,n)+O(T_{\text{transfer}}),$$

де $G_{\text{eff}}\le G$ — ефективно задіяні GPU, а $T_{\text{transfer}}$ агрегує host/device-обміни.

**Прискорення CUDA** відносно CPU-паралельної версії:

$$S_{\text{cuda}}=\frac{T_{P_{\text{mpi}}}}{T_{P_{\text{mpi}},G}},$$

а відносно послідовної версії:

$$S_{\text{cuda,1}}=\frac{T_1}{T_{P_{\text{mpi}},G}}.$$

**Ефективність CUDA** (на ефективний паралельний ресурс):

$$E_{\text{cuda}}=\frac{S_{\text{cuda,1}}}{P_{\text{mpi}}\cdot G_{\text{eff}}}.$$

У режимі великих $k,n$ і перекриття передач обчисленням (streams/pinned memory) домінує обчислювальна частина, і практично:

$$T_{P_{\text{mpi}},G}=\Theta\!\left(\frac{k\cdot R\cdot s}{P_{\text{mpi}}\cdot G_{\text{eff}}}\right).$$

---

## 9. Алгоритм WBC0 / WBC0 Algorithm

### 9.1 Идея алгоритма / Core Idea

WBC0 сохраняет рубиковую концепцию WBC1 (таблица операций `face/slice/wide/cube/swap/diagflip` и их композиции), но использует **каскадный раундовый ключ**: после обработки каждого подблока следующий ключ формируется из первых битов только что полученного шифроподблока.

Алгоритм остаётся обратимым:

$$D_{WBC0}(E_{WBC0}(B_b, K), K) = B_b$$

для любого блока $B_b$ и мастер-ключа $K$.

### 9.2 Параметры и разбиение / Parameters and Splitting

Пусть:
- $B_b \in \{0,1\}^s$ — блок данных длины $s$ бит,
- $K \in \{0,1\}^{k_{key}}$ — мастер-ключ,
- $L = k_{key}/8$ — длина ключа в байтах,
- $m_{\text{shift}} \in \{0,1,2\}$ — режим вычисления шага сдвига,
- $d_{\text{shift}} \in \mathbb{N},\ d_{\text{shift}}\ge 1$ — базовый параметр сдвига,
- $u_{\text{sub}}$ — размер подблока в байтах, $1 \le u_{\text{sub}} \le s/8$.

Для $i$-го шага подблока вводятся:

$$\alpha_i = i \bmod 8,\qquad \beta_i = m_i \bmod 4.$$

Пусть для текущего подблока $X$ его длина в битах равна

$$M = 8|X|_B.$$

Тогда шаг циклического сдвига $\Delta_i$ задаётся по одному из трёх вариантов:

$$
\Delta_i=
\begin{cases}
\begin{cases}
d_{\text{shift}}, & \operatorname{MSB}(m_i)=1,\\
\lfloor d_{\text{shift}}/2\rfloor+1, & \operatorname{MSB}(m_i)=0,
\end{cases} & m_{\text{shift}}=0,\\[6pt]
1+(m_i\bmod d_{\text{shift}}), & m_{\text{shift}}=1,\\[4pt]
(\alpha_i + \beta_i) \bmod M, & m_{\text{shift}}=2.
\end{cases}
$$

Детализация трёх режимов:
- **Режим 0 (baseline):** бинарный выбор шага по старшему биту $m_i$.
- **Режим 1 (uniform key-byte):** квазирaвномерный выбор из диапазона $\{1,\dots,d_{\text{shift}}\}$.
- **Режим 2 (compromise):** базовый шаблон WBC1 ($i\bmod8$) + небольшой ключевой оффсет ($m_i\bmod4$).

Блок разбивается на подблоки:

$$B_b = U_0 || U_1 || \dots || U_{q-1}, \quad |U_r|_B = u_{\text{sub}} \text{ (кроме, возможно, последнего)}$$

### 9.3 Каскадный раундовый ключ / Cascading Round Key

Начальный раундовый ключ:

$$RK^{(0)} = K$$

После шифрования подблока $U_r \to Z_r$ новый ключ строится из битов $Z_r$ циклически до длины мастер-ключа:

$$RK^{(r+1)}[t] = Z_r[t \bmod (8|Z_r|_B)], \quad t=0,1,\dots,8L-1$$

Следовательно, на каждом шаге:

$$|RK^{(r)}| = |K| = k_{key}$$

### 9.4 Выбор операции из байта ключа / Operation Selection per Key Byte

Для каждого байта $RK^{(r)}[i]$ (последовательный проход по $i=0..L-1$) сначала выполняется локальное расширение:

$$z_i = RK[i] \oplus \text{ROTL}_8(RK[i+1], i \bmod 8) \oplus \text{ROTR}_8(RK[i-1], (i+3) \bmod 8) \oplus (29i \bmod 256)$$

затем применяется функция перемешивания:

$$m_i = \text{mix}(z_i) = z_i \oplus (z_i \gg 4) \oplus ((z_i \ll 3) \lor (z_i \gg 5)) \oplus (z_i \gg 1)$$

Индекс операции:

$$j_i = m_i \bmod 127$$

### 9.5 Рубиковая операция как перестановка / Rubik-Style Permutation

Каждая операция $O_{j_i}$ задаёт обратимое отображение байтов подблока и выбирается из той же таблицы классов, что и в WBC1 (`face/slice/wide/cube/swap/diagflip` и dynamic-композиции).

Для дешифрования используется соответствующее обратное отображение $O_{j_i}^{-1}$; для dynamic-операций порядок внутренней цепочки инвертируется.

### 9.6 Шифрование WBC0: короткая и полная формы / WBC0 Encryption: Short and Full Forms

**Функция шифрования всего текста / Full-text encryption function**

$$E_{WBC0}:\{0,1\}^n \times \{0,1\}^{k_{key}} \to \{0,1\}^n$$

$$E_{WBC0}(T,K)=E_{WBC0}(B_0,K)\ ||\ E_{WBC0}(B_1,K)\ ||\ \cdots ||\ E_{WBC0}(B_{k-1},K)$$

где каждый блок $B_b$ обрабатывается независимо (в том числе параллельно между процессами MPI).

**Краткая форма / Short form**

Для блока $B_b$:

$$E_{WBC0}(B_b,K)=\Phi_{RK^{(q-1)}}\circ\cdots\circ\Phi_{RK^{(1)}}\circ\Phi_{RK^{(0)}}(B_b),$$

где $RK^{(0)}=K$, а после каждого подблока $U_r\to Z_r$:

$$RK^{(r+1)}[t]=Z_r[t\bmod(8|Z_r|_B)],\quad t=0,1,\dots,8L-1.$$ 

Для одного подблока $X\in\{0,1\}^{8u_r}$, $u_r=|U_r|_B$:

$$\operatorname{EncSub}_{RK^{(r)}}(X)=\operatorname{Round}_{L-1}^{(r)}\circ\cdots\circ\operatorname{Round}_1^{(r)}\circ\operatorname{Round}_0^{(r)}(X),$$

$$\operatorname{Round}_i^{(r)}(X)=\rho_{\Delta_i}(O_{j_i}(X)),\quad
j_i=m_i\bmod127,$$

$$\Delta_i=
\begin{cases}
\begin{cases}
d_{\text{shift}}, & \operatorname{MSB}(m_i)=1,\\
\lfloor d_{\text{shift}}/2\rfloor+1, & \operatorname{MSB}(m_i)=0,
\end{cases} & m_{\text{shift}}=0,\\[6pt]
1+(m_i\bmod d_{\text{shift}}), & m_{\text{shift}}=1,\\[4pt]
(\alpha_i+\beta_i)\bmod M, & m_{\text{shift}}=2,
\end{cases}
\quad \alpha_i=i\bmod 8,
\quad \beta_i=m_i\bmod 4,
\quad M=8u_r.$$ 

где $\rho_{\Delta_i}:\{0,1\}^{8u_r}\to\{0,1\}^{8u_r}$.

**Полная форма / Full form**

$$
\begin{aligned}
&\textbf{Вход:}\ B_b\in\{0,1\}^{s},\ K\in\{0,1\}^{k_{key}}\\
&\textbf{Выход:}\ C_b\in\{0,1\}^{s}\\
&1.\ RK^{(0)}\leftarrow K\\
&2.\ Разбить\ B_b\ на\ U_0,\ldots,U_{q-1},\ \ |U_r|_B=u_r\\
&3.\ \textbf{Для } r=0,1,\ldots,q-1:\\
&\quad 3.1\ X\leftarrow U_r\\
&\quad 3.2\ \textbf{Для } i=0,1,\ldots,L-1:\\
&\qquad z_i\leftarrow RK^{(r)}[i]\oplus \operatorname{ROTL}_8(RK^{(r)}[i+1], i\bmod 8)\\
&\qquad\ \ \ \ \ \oplus \operatorname{ROTR}_8(RK^{(r)}[i-1], (i+3)\bmod 8)\oplus (29i\bmod 256)\\
&\qquad m_i\leftarrow \operatorname{mix}(z_i)\\
&\qquad j_i\leftarrow m_i\bmod 127\\
&\qquad Y\leftarrow O_{j_i}(X)\\
&\qquad \textbf{if }m_{\text{shift}}=0:\\
&\qquad\quad \Delta_i\leftarrow
\begin{cases}
d_{\text{shift}}, & \operatorname{MSB}(m_i)=1,\\
\lfloor d_{\text{shift}}/2\rfloor+1, & \operatorname{MSB}(m_i)=0
\end{cases}\\
&\qquad \textbf{else if }m_{\text{shift}}=1:\\
&\qquad\quad \Delta_i\leftarrow 1+(m_i\bmod d_{\text{shift}})\\
&\qquad \textbf{else }(m_{\text{shift}}=2):\\
&\qquad\quad \alpha_i\leftarrow i\bmod 8\\
&\qquad\quad \beta_i\leftarrow m_i\bmod 4\\
&\qquad\quad \Delta_i\leftarrow (\alpha_i+\beta_i)\bmod (8u_r)\\
&\qquad X\leftarrow \rho_{\Delta_i}(Y)\\
&\quad 3.3\ Z_r\leftarrow X\\
&\quad 3.4\ RK^{(r+1)}[t]\leftarrow Z_r[t\bmod(8|Z_r|_B)],\ t=0,1,\dots,8L-1\\
&4.\ C_b\leftarrow Z_0||Z_1||\cdots||Z_{q-1}\\
&5.\ \textbf{вернуть } C_b
\end{aligned}
$$

Это сохраняет принцип: **«элемент ключа → операция из таблицы → циклический сдвиг»**.

### 9.7 Дешифрование WBC0: короткая и полная формы / WBC0 Decryption: Short and Full Forms

**Краткая форма / Short form**

При известных $\{j_i,\Delta_i\}_{i=0}^{L-1}$ для раундового ключа $RK^{(r)}$:

$$\operatorname{DecSub}_{RK^{(r)}}(Z_r)=\operatorname{InvRound}_0^{(r)}\circ\operatorname{InvRound}_1^{(r)}\circ\cdots\circ\operatorname{InvRound}_{L-1}^{(r)}(Z_r),$$

$$\operatorname{InvRound}_i^{(r)}(X)=O_{j_i}^{-1}(\rho_{-\Delta_i}(X)).$$

Эквивалентно итерации в цикле $i=L-1,\ldots,0$:

$$X\leftarrow\rho_{-\Delta_i}(X),\qquad X\leftarrow O_{j_i}^{-1}(X).$$

**Полная форма / Full form**

$$
\begin{aligned}
&\textbf{Вход:}\ C_b\in\{0,1\}^{s},\ K\in\{0,1\}^{k_{key}}\\
&\textbf{Выход:}\ B_b\in\{0,1\}^{s}\\
&1.\ Восстановить\ последовательность\ раундовых\ ключей\ RK^{(0)},\ldots,RK^{(q-1)}\\
&\quad \text{по тем же правилам, что и при шифровании (через соответствующие }Z_r\text{)}\\
&2.\ Разбить\ C_b\ на\ Z_0,\ldots,Z_{q-1}\\
&3.\ \textbf{Для } r=q-1,q-2,\ldots,0:\\
&\quad 3.1\ X\leftarrow Z_r\\
&\quad 3.2\ \text{По }RK^{(r)}\text{ вычислить }\{j_i,\Delta_i\}_{i=0}^{L-1}\\
&\quad 3.3\ \textbf{Для } i=L-1,L-2,\ldots,0:\\
&\qquad X\leftarrow \rho_{-\Delta_i}(X)\\
&\qquad X\leftarrow O_{j_i}^{-1}(X)\\
&\quad 3.4\ U_r\leftarrow X\\
&4.\ B_b\leftarrow U_0||U_1||\cdots||U_{q-1}\\
&5.\ \textbf{вернуть } B_b
\end{aligned}
$$

После обработки подблока обновление каскадного ключа выполняется той же функцией от соответствующего шифроподблока $Z_r$, что согласует encrypt/decrypt.

### 9.8 Итоговая формула блока / Block-Level Form

Пусть $\Phi_{RK^{(r)}}=\operatorname{EncSub}_{RK^{(r)}}$ — преобразование из раздела 8.6 для $r$-го подблока. Тогда для блока:

$$E_{WBC0}(B_b, K) = \Phi_{RK^{(q-1)}} \circ \cdots \circ \Phi_{RK^{(1)}} \circ \Phi_{RK^{(0)}}(B_b)$$

где $RK^{(r+1)} = \text{CascadeKey}(Z_r)$, $Z_r$ — результат шифрования $r$-го подблока.

### 9.9 Параллельное распределение блоков в WBC0 / Parallel Block Distribution in WBC0

В реализации WBC0 используется модель MPI с распределением блоков по процессам через `Scatterv/Gatherv`.

Пусть:
- $N_b$ — число блоков сообщения,
- $P$ — число MPI-процессов,
- $p \in \{0,1,\dots,P-1\}$ — индекс процесса.

Тогда вычисляются:

$$\text{base} = \left\lfloor \frac{N_b}{P} \right\rfloor, \qquad \text{extra} = N_b \bmod P$$

Количество блоков у процесса $p$:

$$n_p = \text{base} + \begin{cases}
1, & p < \text{extra}\\
0, & p \ge \text{extra}
\end{cases}$$

То есть в общем случае выполняется правило:

$$n_p \in \left\{\left\lfloor \frac{N_b}{P} \right\rfloor,\; \left\lceil \frac{N_b}{P} \right\rceil\right\}$$

и каждый процесс шифрует/дешифрует **пакет блоков**, а не обязательно ровно один блок.

Случай «один блок на процесс» является частным и возникает только когда $N_b \le P$ (или близко к этому, в зависимости от распределения остатка).

После локальной обработки на каждом процессе:

$$C = C^{(0)} || C^{(1)} || \cdots || C^{(P-1)}$$

собирается на root-процессе в исходном порядке блоков с использованием векторов `sendcounts` и `displs`.

Следовательно, параллельная сложность по времени для вычислительной части имеет вид:

$$T_{\parallel} = O\!\left(\left\lceil \frac{N_b}{P} \right\rceil \cdot C_{\text{block}}\right)$$

где $C_{\text{block}}$ — стоимость шифрования одного блока алгоритмом WBC0.

### 9.10 CUDA-ускоренный вариант WBC0 / CUDA-Accelerated WBC0 Variant

В CUDA-варианте ускоряется операция циклического побитового сдвига $\rho_{\Delta}$, оставаясь математически эквивалентной CPU-версии.

Пусть подблок $X \in \{0,1\}^{8u}$, где $u$ — размер подблока в байтах, и $M=8u$.

Для каждого бита назначения $t \in \{0,1,\dots,M-1\}$ определяется источник:

$$s(t) = (t + \Delta) \bmod M$$

и вычисляется:

$$X' [t] = X[s(t)]$$

Это в точности соответствует определению:

$$X' = \rho_{\Delta}(X)$$

Следовательно, замена CPU-вычисления сдвига на GPU-ядро не изменяет криптографическую функцию $E_{WBC0}$, а изменяет только время выполнения.

### 9.11 Параллельная карта потоков CUDA / CUDA Thread Mapping

В ядре CUDA каждому биту назначения $t$ сопоставляется один поток:

$$\text{thread}(t) \Rightarrow X'[t]$$

Тем самым вычисление $\rho_{\Delta}$ выполняется за один проход по $M$ битам с массовым параллелизмом.

При использовании предвычисленных карт индексов (lookup map) переходы

$$t \mapsto s(t),\quad t \mapsto (\text{dst\_byte},\text{dst\_bit})$$

не вычисляются повторно для каждого вызова, что снижает накладные расходы.

### 9.12 Гибрид MPI + CUDA / Hybrid MPI + CUDA

Пусть на узле выполняется $P$ MPI-процессов, а доступно $G$ GPU-устройств.
Процессу $p$ назначается устройство:

$$g(p) = p \bmod G$$

Далее каждый процесс обрабатывает свой пакет блоков (раздел 8.9), а внутри обработки блока применяет GPU-ускоренный сдвиг.

Итоговая схема:

1. межпроцессное распределение блоков: MPI Scatterv/Gatherv;
2. внутрипроцессное ускорение шага $\rho_{\Delta}$: CUDA.

Таким образом, достигается двухуровневый параллелизм: межблочный (MPI) и внутриблочный (CUDA).

### 9.13 Оценка сложности CUDA-варианта / Complexity of CUDA Variant

Общую стоимость можно представить как:

$$T_{\text{hybrid}} \approx T_{\text{MPI-dist}} + T_{\text{GPU-kernel}} + T_{\text{transfer}}$$

где:
- $T_{\text{MPI-dist}}$ — распределение/сбор блоков между процессами;
- $T_{\text{GPU-kernel}}$ — вычисление сдвига и связанных операций на GPU;
- $T_{\text{transfer}}$ — обмен между host и device.

При больших $N_b$ и достаточной загрузке GPU обычно выполняется:

$$T_{\text{GPU-kernel}} \ll T_{\text{CPU-shift}}$$

что даёт ускорение без изменения математической структуры алгоритма:

$$E^{\text{CUDA}}_{WBC0}(B_b,K) \equiv E_{WBC0}(B_b,K)$$

при условии эквивалентной реализации операций $O_{j_i}$, $\rho_{\Delta_i}$ и каскадного обновления ключа.

### 9.14 Уточнённая сложность для `wbc0_original_parallel_cuda.cu` / Refined Complexity for `wbc0_original_parallel_cuda.cu`

Ниже — оценка, согласованная с текущей реализацией `wbc0_original_parallel_cuda.cu`.

Пусть:
- $n$ — длина входных данных в битах, $s$ — размер блока в битах, $k=\lceil n/s\rceil$ — число блоков;
- $m=l/8$ — длина мастер-ключа в байтах;
- $u$ — размер подблока в байтах (`subblock_size`),
- $q=\left\lceil\frac{s/8}{u}\right\rceil$ — число подблоков внутри одного блока;
- $P$ — число MPI-процессов, $G$ — число задействованных GPU.

**Ключевая особенность реализации:** CUDA используется для `cyclic_bitwise_shift`, а операции перестановки $O_j$ и обновление каскадного ключа выполняются на CPU.

**Сложность одного подблока.**

Для каждого из $m$ шагов подблока выполняются:
- `apply_operation_sized(...)` на CPU: $\Theta(u)$;
- `cyclic_bitwise_shift(...)` (GPU/CPU-fallback): асимптотически $\Theta(u)$;
- константные вычисления `op_id`/`shift`.

Следовательно,

$$T_{\text{sub}}=\Theta(m\cdot u).$$

**Сложность одного блока.**

В блоке обрабатывается $q$ подблоков:

$$T_{\text{block}}=\Theta(q\cdot m\cdot u)=\Theta\!\left(m\cdot\frac{s}{8}\right)=\Theta(m\cdot s).$$

(так как $q\cdot u\approx s/8$).

**Последовательная сложность.**

$$T_1=\Theta(k\cdot m\cdot s).$$

Это совпадает с оценкой для CPU-версии WBC0: перенос шага `cyclic_bitwise_shift` на GPU **не меняет** асимптотическую трудоёмкость самого алгоритма (по числу элементарных операций над данными), а меняет только константы времени выполнения и добавляет служебные операции CUDA.

Эквивалентно по «работе» (total work):

$$W_{\text{WBC0-CUDA}}=\Theta(k\cdot m\cdot s)=W_{\text{WBC0-CPU}}.$$ 

**MPI + CUDA (время выполнения).**

Каждый MPI-процесс обрабатывает

$$n_p\in\left\{\left\lfloor\frac{k}{P}\right\rfloor,\left\lceil\frac{k}{P}\right\rceil\right\}$$

блоков. При распределении работы между $P\cdot G$ вычислительными единицами вычислительная часть времени оценивается как

$$T_{\text{calc}}=\Theta\!\left(\left\lceil\frac{k}{P\cdot G}\right\rceil\,m\,(b+q)\right),$$

где $b=s/8$.

Полная модель времени (с накладными расходами MPI и CUDA):

$$
\begin{aligned}
T_{P,G} &= T_{\text{calc}}+T_{\text{sync}}+T_{\text{comm}}+T_{\text{H2D/D2H}}+T_{\text{launch}} \\
&= O\!\left(\left\lceil\frac{k}{P\cdot G}\right\rceil m(b+q)\right)
+ O(\tau_c\,t\,\log P)
+ O(\tau_o\,t\,n) \\
&\quad + O\!\left(\left\lceil\frac{k}{P}\right\rceil(\alpha_{\text{pcie}}+\beta_{\text{pcie}}\,b)\right)
+ O\!\left(\left\lceil\frac{k}{P}\right\rceil q\,\alpha_k\right) \\
&= O\!\left(\left\lceil\frac{k}{P\cdot G}\right\rceil m(b+q)\right)
+ O(\tau_c\,t\,\log P)
+ O(\tau_o\,t\,n) \\
&\quad + O\!\left(\left\lceil\frac{k}{P}\right\rceil(\alpha_{\text{pcie}}+\beta_{\text{pcie}}\,b+q\,\alpha_k)\right).
\end{aligned}
$$

где $\alpha_{\text{pcie}}$ — латентность шины, $\beta_{\text{pcie}}$ — обратная пропускная способность, $\alpha_k$ — латентность запуска CUDA-ядра.

**Ускорение и эффективность CUDA-версии.**

Последовательное время:

$$T_1=\Theta\!\left(k\,m\,(b+q)\right).$$

Ускорение:

$$S_{P,G}=\frac{T_1}{T_{P,G}}.$$

Эффективность гибридной схемы:

$$E_{P,G}=\frac{S_{P,G}}{P\cdot G}.$$

Для больших $k$ (при фиксированных $P,G$) фиксированные накладные расходы амортизируются, а при росте $q$ вклад запуска ядер $q\alpha_k$ может начать доминировать.

Оценка критического значения (эвристика баланса compute/launch):

$$q_{\text{crit}}\approx\frac{m\,b}{\alpha_k\,\theta_{\text{GPU}}},$$

где $\theta_{\text{GPU}}$ — эффективная пропускная способность GPU.

## 10. Стійкість до криптоатак / Resistance to Cryptanalytic Attacks

### 10.1 Модель оцінювання та обмеження / Evaluation Model and Limits

Нижче наведена **інженерна** (не формально-доведена) оцінка стійкості для реалізацій сімейства WBC.

Приймаються такі умови:
- суперник має доступ до пар відкритий текст/шифротекст (CPA/частково KPA-модель);
- внутрішня таблиця операцій і параметри реалізації можуть бути відомі (Kerckhoffs);
- секретним залишається лише ключ;
- оцінка стосується блочного перетворення, а не режиму шифрування файлу загалом.

Важливо: без формальних доведень і широкого публічного криптоаналізу жоден з алгоритмів не слід трактувати як еквівалент стандартів на кшталт AES.

### 10.2 WBC1

**Сильні сторони:**
- ключезалежний вибір операцій $O_j$;
- добрий емпіричний лавинний ефект;
- повна оборотність перетворень.

**Потенційні ризики:**
- раунд складається лише з перестановок/зсувів, тобто структура близька до афінної над бітами;
- відсутність S-box та криптографічного key schedule зменшує нелінійність;
- для моделей з обраним відкритим текстом можливі спрощені диференціальні/лінійні наближення порівняно з WBC2.

Підсумок: стійкість **базового рівня**, придатна для експериментального/інженерного використання, але з вищим ризиком криптоаналітичних спрощень, ніж у WBC2/PWBC2.1.

### 10.3 WBC2

**Покращення відносно WBC1:**
- раундові ключі через SHA-256;
- ключезалежний S-box;
- посилена дифузія (додаткові шари перемішування).

Це ускладнює:
- лінійний криптоаналіз (через нелінійність S-box),
- диференціальний криптоаналіз (через багатошарову дифузію),
- атаки пов'язаних ключів (через хеш-генерацію раундових ключів).

Підсумок: найвища криптостійкість серед базових WBC1/WBC2 за поточною архітектурою, але все ще без формального security proof.

### 10.4 PWBC2.1

PWBC2.1 (для `wbc1_parallel_cached`) додає до пермутації $O_{j_r}$:
- XOR із раундовим ключем,
- S-box,
- префіксну XOR-дифузію,
- побайтовий циклічний зсув.

**Очікувана стійкість:**
- вища за WBC1 завдяки нелінійності та раундовим ключам;
- близька до WBC2 за класом операцій, але залежить від числа раундів $R$ та параметризації дифузії;
- при малих $R$ можливе зниження запасу стійкості проти багатораундових диференціальних наближень.

Практична рекомендація: використовувати достатній $R$ (не мінімальні значення) і перевіряти лавинний/диференціальний профіль на цільових параметрах.

### 10.5 WBC0

WBC0 використовує каскадне оновлення раундового ключа від уже зашифрованого підблоку, що:
- підсилює залежність наступних перетворень від попереднього шифротексту;
- ускладнює прості незалежні атаки на окремі кроки підблоку.

Водночас:
- з'являється складніша залежна структура стану між підблоками;
- без формального аналізу важко дати строгі межі стійкості до адаптивних атак з обраним текстом.

Підсумок: стійкість очікується **не нижча за WBC1** і може бути вищою в частині key-dependency, але потребує окремого формального криптоаналізу.

### 10.6 CUDA-варіант

Для `wbc0_original_parallel_cuda.cu` криптографічне перетворення еквівалентне CPU-версії (прискорюється лише обчислення циклічного зсуву), тому:

$$E_{WBC0}^{\text{CUDA}}(B,K)\equiv E_{WBC0}(B,K).$$

Отже, **математична криптостійкість не змінюється**.

Але додається поверхня реалізаційних ризиків:
- витоки через таймінг/мікроархітектурні ефекти GPU;
- залишки даних у device-memory при некоректному очищенні;
- ризики multi-tenant середовищ (спільний GPU);
- помилки синхронізації/копіювання, що можуть порушити модель загроз.

### 10.7 Порівняльний висновок / Comparative Takeaway

У порядку очікуваної криптостійкості (за поточною архітектурою):

$$\text{WBC1} \lesssim \text{WBC0} \lesssim \text{PWBC2.1} \approx \text{WBC2}.$$

CUDA-варіант не підвищує і не знижує теоретичну стійкість алгоритму, а змінює лише продуктивність і реалізаційний профіль ризиків.

### 10.8 Порівняльна таблиця алгоритмів / Comparative Table of Algorithms

| Алгоритм | Кількість раундів (на блок) | Час шифрування (приклад для 1000 КБ даних) | Статистичні характеристики | Ключові особливості |
|---|---|---|---|---|
| **WBC1** | $m=l/8$ | Орієнтир MPI з документа для обсягу 1000 КБ: $P=1$ — ~40.0s, $P=8$ — ~5.3s | Лавинний ефект: ~48–52%; чутливість до ключа: ~45–50% | Перестановки $O_j$ + циклічні зсуви; висока швидкість, нижча нелінійність |
| **WBC2** | $r+1$ (раунд 0 + раунди $1..r$) | Орієнтовно повільніше за WBC1 (~4.5 сек/МБ проти ~4 сек/МБ); для 1000 КБ уніфікований окремий замір у файлі не наведений | Лавинний ефект: ~50–54%; чутливість до ключа: ~48–52% | Раундові ключі SHA-256, S-box, посилена дифузія |
| **PWBC2.1** | $R$ | н/д (у поточному документі немає окремого єдиного бенчмарку для 1000 КБ) | н/д (очікувано близько до WBC2 за класом операцій) | $O_{j_r}$ + XOR + S-box + префіксна XOR-дифузія + $\mathrm{ROTR}_8$ |
| **WBC0** | $q\cdot m$, де $q=\left\lceil\frac{s/8}{u}\right\rceil$ | н/д (немає уніфікованого заміру для 1000 КБ у цьому файлі) | н/д (у тексті якісна оцінка без числового профілю) | Каскадне оновлення раундового ключа між підблоками |
| **WBC0 (CUDA)** (`wbc0_original_parallel_cuda.cu`) | $q\cdot m$ (така ж криптологічна структура, як у WBC0) | Залежить від GPU/PCIe; у моделі: прискорення за рахунок `cyclic_bitwise_shift`, при малих задачах домінує overhead | Математично ті самі, що у WBC0 | Еквівалентне шифрування до CPU-версії, але інший performance-профіль і реалізаційні ризики |

Примітка: значення часу для 1000 КБ даних наведені як **орієнтири з наявних у документі вимірювань**; для строгої міжалгоритмічної таблиці потрібен єдиний benchmark-протокол (однакові дані, параметри, $P$, апаратна платформа, режими).

---

**Конец документа / End of Document**
