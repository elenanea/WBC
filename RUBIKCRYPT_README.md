# RubikCrypt

**RubikCrypt** — веб-приложение для шифрования текста и файлов на основе семейства алгоритмов White-Box Cryptography (WBC). Поддерживает как последовательные, так и параллельные (MPI) реализации алгоритмов с настраиваемыми режимами шифрования.

---

## Содержание

1. [Архитектура приложения](#архитектура-приложения)
2. [Реализованные алгоритмы](#реализованные-алгоритмы)
   - [Последовательные](#последовательные-алгоритмы)
   - [Параллельные (MPI)](#параллельные-алгоритмы-mpi)
3. [Режимы шифрования](#режимы-шифрования)
4. [Руководство пользователя](#руководство-пользователя)
   - [Интерфейс](#интерфейс)
   - [Шифрование текста](#шифрование-текста)
   - [Расшифрование текста](#расшифрование-текста)
   - [Шифрование файла](#шифрование-файла)
   - [Управление ключом](#управление-ключом)
5. [Технические детали](#технические-детали)
6. [Зависимости и запуск](#зависимости-и-запуск)

---

## Архитектура приложения

```
webapp/
├── app.py                    # Flask-сервер, маршрутизация, вызов CLI-обёрток
├── templates/
│   └── index.html            # Веб-интерфейс (HTML/CSS/JS, БЕЗ фреймворков)
├── cli_wbc1_casc             # CLI-обёртка WBC1.0   (бинарный файл)
├── cli_wbc1_casc.c           # исходный код обёртки WBC1.0
├── cli_wbc1_nomix            # CLI-обёртка WBC1     (бинарный файл)
├── cli_wbc1_nomix.c          # исходный код обёртки WBC1
├── cli_wbc1_casc2            # CLI-обёртка WBC1.1   (бинарный файл)
├── cli_wbc1_casc2.c          # исходный код обёртки WBC1.1
├── cli_wbc2_serial           # CLI-обёртка WBC2     (бинарный файл)
├── cli_wbc2_serial.c         # исходный код обёртки WBC2
├── cli_pwbc0                 # CLI-обёртка PWBC1.1  (MPI бинарный файл)
├── cli_pwbc0.c               # исходный код обёртки PWBC1.1
├── cli_pwbc1                 # CLI-обёртка PWBC1    (MPI бинарный файл)
├── cli_pwbc1.c               # исходный код обёртки PWBC1
├── cli_pwbc2                 # CLI-обёртка PWBC2    (MPI бинарный файл)
├── cli_pwbc2.c               # исходный код обёртки PWBC2
├── cli_pwbc21                # CLI-обёртка PWBC2.1  (MPI бинарный файл)
├── cli_pwbc21.c              # исходный код обёртки PWBC2.1
└── cli_wbc_serial_common.h   # Общий заголовок для последовательных обёрток
                               # (разбор enc_mode, WBCM-фрейминг, IV, PKCS7)
```

Flask-сервер (`app.py`) получает HTTP-запрос, вызывает нужный CLI-бинарник через `subprocess`, передаёт ключ и данные через stdin, читает зашифрованный/расшифрованный результат из stdout.

Параллельные алгоритмы запускаются через:
```
mpirun --allow-run-as-root --oversubscribe -n <nprocs> ./cli_pwbc*
```

---

## Реализованные алгоритмы

### Последовательные алгоритмы

#### WBC1
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `WBC1` |
| **Исходный файл алгоритма** | `wbc1_cascade_no_mix_FIXED.c` |
| **Исходный файл обёртки** | `webapp/cli_wbc1_nomix.c` |
| **Бинарный файл** | `webapp/cli_wbc1_nomix` |
| **Тип** | Последовательный, блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | Каскадный WBC1 **без перемешивания ключа** — упрощённая версия: ключ эволюционирует, но блоки шифруются независимо (без обратной связи через предыдущий блок). Менее стойкий к статистическому анализу, но быстрее |
| **Поддерживаемые режимы** | CTR\_HMAC, ECB, CBC, CTR, OFB, CFB |

---

#### WBC1.0
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `WBC1.0` |
| **Исходный файл алгоритма** | `wbc1_fixed_cascade.c` |
| **Исходный файл обёртки** | `webapp/cli_wbc1_casc.c` |
| **Бинарный файл** | `webapp/cli_wbc1_casc` |
| **Тип** | Последовательный, блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | Каскадный WBC1 **с перемешиванием ключа** — после шифрования каждого блока ключ эволюционирует (мутирует на основе зашифрованного блока), что обеспечивает зависимость каждого блока от всех предыдущих |
| **Поддерживаемые режимы** | CTR\_HMAC, ECB, CBC, CTR, OFB, CFB |

---

#### WBC1.1
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `WBC1.1` |
| **Исходный файл алгоритма** | `wbc1_fixed_cascade2.c` |
| **Исходный файл обёртки** | `webapp/cli_wbc1_casc2.c` |
| **Бинарный файл** | `webapp/cli_wbc1_casc2` |
| **Тип** | Последовательный, блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | Каскадный WBC1 версии 2 (`VERSION_CASC=0x02`) — **двойной каскад**: Pass 1 — прямой каскад с эволюцией ключа (режим ECB/CBC/CTR/OFB/CFB), Pass 2 — дополнительный обратный ECB-каскад по зашифрованным блокам. Второй проход существенно усиливает диффузию |
| **Поддерживаемые режимы** | CTR\_HMAC, ECB, CBC, CTR, OFB, CFB |

---

#### WBC2
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `WBC2` |
| **Исходный файл алгоритма** | `wbc2_serial.c` |
| **Исходный файл обёртки** | `webapp/cli_wbc2_serial.c` |
| **Бинарный файл** | `webapp/cli_wbc2_serial` |
| **Тип** | Последовательный, блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Число раундов** | 32 |
| **Принцип работы** | Последовательная версия PWBC2.1: **127 операций Кубика Рубика** (87 базовых + 20 динамических + 127 цепочек, зависящих от ключа через MT19937), ключезависимый **S-box** (256 элементов), **XOR** с раундовым ключом, **накопительное XOR-рассеивание** (прямое при шифровании / обратное при расшифровании), **циклический сдвиг байт** вправо. KDF: SHA-512(master\_key ∥ nonce) → key\_enc (32 байта) + key\_mac (32 байта). HMAC-SHA256 для аутентификации в режиме CTR\_HMAC |
| **Поддерживаемые режимы** | CTR\_HMAC, ECB, CBC, CTR, OFB, CFB |

---

### Параллельные алгоритмы (MPI)

> Все параллельные алгоритмы используют OpenMPI. Данные автоматически разбиваются на блоки и распределяются по MPI-процессам. Результат собирается на ранге 0 и записывается в stdout.

#### PWBC1
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `PWBC1` |
| **Исходный файл алгоритма** | `wbc1_original_parallel.c` |
| **Исходный файл обёртки** | `webapp/cli_pwbc1.c` |
| **Бинарный файл** | `webapp/cli_pwbc1` |
| **Тип** | Параллельный (MPI), блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | Улучшенный параллельный WBC1: 32 раунда, оптимизированная обработка подблоков. Более высокая криптографическая стойкость за счёт удвоенного числа раундов |
| **Рекомендуемый nprocs** | 4–8 |

---

#### PWBC1.1
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `PWBC1.1` |
| **Исходный файл алгоритма** | `wbc0_original_parallel.c` |
| **Исходный файл обёртки** | `webapp/cli_pwbc0.c` |
| **Бинарный файл** | `webapp/cli_pwbc0` |
| **Тип** | Параллельный (MPI), блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | Оригинальный параллельный WBC1: 16 раундов, внутренние операции над 3D-кубическим блоком. Каждый процесс шифрует свою часть блоков независимо (аналог ECB, но в параллельном пространстве) |
| **Режим сдвига (`shift_mode`)** | 0 = Baseline (скользящий сдвиг), 1 = Uniform key-byte (сдвиг по ключевому байту), 2 = Alpha+Beta (компромиссная схема) |
| **Рекомендуемый nprocs** | 4–8 |

---

#### PWBC2
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `PWBC2` |
| **Исходный файл алгоритма** | `wbc2_original_parallel.c` |
| **Исходный файл обёртки** | `webapp/cli_pwbc2.c` |
| **Бинарный файл** | `webapp/cli_pwbc2` |
| **Тип** | Параллельный (MPI), блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | WBC2: архитектура Фейстеля с ключезависимым S-box, XOR-раундами и двухслойной диффузией. Число раундов настраивается |
| **Число раундов** | 1–100 (по умолчанию 10) |
| **Рекомендуемый nprocs** | 4–8 |

---

#### PWBC2.1
| Параметр | Значение |
|---|---|
| **Идентификатор в UI** | `PWBC2.1` |
| **Исходный файл алгоритма** | `wbc1_parallel_cached.c` |
| **Исходный файл обёртки** | `webapp/cli_pwbc21.c` |
| **Бинарный файл** | `webapp/cli_pwbc21` |
| **Тип** | Параллельный (MPI), блочный |
| **Размер блока** | 128 бит (16 байт) |
| **Размер ключа** | 256 бит (32 байта) |
| **Принцип работы** | WBC1 с кешированием операций: предварительно вычисленные таблицы замен ускоряют обработку блоков. Два режима алгоритма |
| **Режим алгоритма** | `FULL` (5 операций, 16 раундов, максимальная стойкость) / `SIMPLIFIED` (2 операции, 8 раундов, максимальная скорость) |
| **Рекомендуемый nprocs** | 4–8 |

---

## Режимы шифрования

Применяются только к **последовательным** алгоритмам (WBC1, WBC2, WBC1.0, WBC1.1).

| Режим | Описание | IV | MAC |
|---|---|---|---|
| **CTR\_HMAC** | Счётчик (CTR) + HMAC-SHA256 для аутентификации. **Рекомендуется по умолчанию** | Да | Да |
| **ECB** | Electronic Codebook — каждый блок шифруется независимо | Нет | Нет |
| **CBC** | Cipher Block Chaining — каждый блок XOR-ится с предыдущим шифртекстом | Да | Нет |
| **CTR** | Counter Mode — блочный шифр используется как поточный | Да | Нет |
| **OFB** | Output Feedback — гамма генерируется независимо от данных | Да | Нет |
| **CFB** | Cipher Feedback — поточный режим с обратной связью по шифртексту | Да | Нет |

**Формат выходных данных (бинарный):**

| Режим | Формат |
|---|---|
| CTR\_HMAC | `nonce (12 байт) \| magic (4 байта) \| ver (1 байт) \| block_size BE (2 байта) \| ciphertext \| HMAC-SHA256 (32 байта)` |
| ECB | `block_size BE (2 байта) \| ciphertext` |
| CBC / CTR / OFB / CFB | `IV (16 байт) \| block_size BE (2 байта) \| ciphertext` |

Режим шифрования передаётся в бинарник через **argv[1]** и **не хранится** в зашифрованных данных — при расшифровании необходимо указывать тот же режим.

---

## Руководство пользователя

### Интерфейс

После открытия страницы вы увидите:

```
┌─────────────────────────────────────────────────────┐
│  [🎲 логотип]  RubikCrypt                           │
│  White-Box Cryptography — шифрование данных WBC     │
├─────────────────────────────────────────────────────┤
│  [WBC1]  [WBC2]  [WBC1.0]  [WBC1.1]                 │  ← Выбор алгоритма
│  [PWBC1] [PWBC1.1] [PWBC2] [PWBC2.1]                │
├─────────────────────────────────────────────────────┤
│  [🔒 Шифровать] [🔓 Расшифровать]                  │  ← Режим работы
├─────────────────────────────────────────────────────┤
│  Настройки: [Режим шифрования ▼] [Режим алг. ▼]    │  ← Параметры алгоритма
│             [Процессов MPI ──●──]                   │
│             [Раундов PWBC2  ──●──]                  │
├─────────────────────────────────────────────────────┤
│  Ключ: [________________________________] [⚙] [📋] │
├─────────────────────────────────────────────────────┤
│  [📝 Текст] [📄 Файл]    │  Результат (предпросмотр)│
│  Введите текст...        │                          │
│  HEX: 48 65 6c 6c 6f... │                          │
├─────────────────────────────────────────────────────┤
│       [🔒 Зашифровать]                              │
└─────────────────────────────────────────────────────┘
```

---

### Шифрование текста

1. **Выберите алгоритм** — кликните на карточку алгоритма (например, `WBC1`).

2. **Настройте параметры** (появляются автоматически для выбранного алгоритма):
   - Для WBC1/WBC2/WBC1.0/WBC1.1 — выпадающий список **«Режим шифрования»** (рекомендуется `CTR_HMAC`).
   - Для PWBC1 — выпадающий список **«Режим сдвига»** (0 = Baseline, 1 = Uniform, 2 = Alpha+Beta).
   - Для PWBC2 — слайдер **«Раундов PWBC2»** (по умолчанию 10).
   - Для PWBC2.1 — выпадающий список **«Режим алгоритма»** (FULL или SIMPLIFIED).
   - Для всех параллельных — слайдер **«Процессов MPI»** (1–8, рекомендуется 4).

3. **Введите или сгенерируйте ключ**:
   - Нажмите **⚙ Генерировать** для случайного 256-битного ключа.
   - Или введите вручную 64 шестнадцатеричных символа (0–9, a–f).
   - Ключ сохраните — он потребуется для расшифрования.

4. **Убедитесь, что выбрана вкладка «📝 Текст»**, введите открытый текст.
   - Под полем ввода в реальном времени отображается **HEX-представление** введённого текста.

5. Нажмите **🔒 Зашифровать**.

6. **Результат** отображается в двух форматах:
   - **HEX** — шестнадцатеричная строка
   - **Base64** — компактная строка для передачи в текстовых протоколах
   - Используйте **📋 Копировать HEX** или **📋 Копировать Base64** для сохранения.

---

### Расшифрование текста

1. Нажмите вкладку **🔓 Расшифровать**.

2. Выберите **тот же алгоритм**, которым шифровали.

3. Установите **те же параметры** (shift_mode / algo_mode / num_rounds).

4. Введите **тот же ключ**.

5. В поле ввода вставьте **HEX-строку** зашифрованных данных (скопированную на шаге «Копировать HEX»).
   > Пробелы в HEX-строке допустимы — они автоматически удаляются.

6. Нажмите **🔓 Расшифровать** — появится расшифрованный текст.

---

### Шифрование файла

1. Нажмите **📄 Файл** для переключения на файловый режим.

2. Перетащите файл в зону загрузки или нажмите на неё для выбора файла.

3. Настройте алгоритм, параметры и ключ (аналогично текстовому режиму).

4. Нажмите **🔒 Зашифровать** — появится кнопка **⬇ Скачать файл**.

5. Зашифрованный файл будет иметь расширение `.rubikcrypt`.

**Расшифрование файла:**

1. Переключитесь на вкладку **🔓 Расшифровать**.
2. Загрузите файл `.rubikcrypt`.
3. Укажите алгоритм, параметры и ключ.
4. Нажмите **🔓 Расшифровать** — скачается исходный файл без расширения `.rubikcrypt`.

---

### Управление ключом

| Действие | Описание |
|---|---|
| **⚙ Генерировать** | Генерирует криптографически стойкий случайный 256-битный ключ через `/dev/urandom` (серверная сторона) |
| **📋 Копировать** | Копирует ключ в буфер обмена |
| Ввод вручную | 64 символа из диапазона `0-9`, `a-f` (A-F тоже допустимы) |

> ⚠️ **Важно:** ключ не сохраняется на сервере. Если ключ утрачен — расшифрование невозможно.

---

## Технические детали

### Протокол CLI-обёрток

**Последовательные алгоритмы (WBC1, WBC2, WBC1.0, WBC1.1):**
```
argv[1]: <enc_mode>          # CTR_HMAC | ECB | CBC | CTR | OFB | CFB

stdin:
  line 1: <hex_key>          # 64 символа
  line 2: e|d                # e = encrypt, d = decrypt
  <binary data>              # входные данные

stdout:
  <binary data>              # результат
```

**Параллельные алгоритмы PWBC1, PWBC1.1:**
```
stdin:
  line 1: <hex_key>
  line 2: e|d
  <binary data>
```

**PWBC1.1 (дополнительно читает):**
```
  line 3: <shift_mode>       # 0 | 1 | 2
```

**PWBC2 (дополнительно читает):**
```
  line 3: <num_rounds>       # 1–100
```

**PWBC2.1 (дополнительно читает):**
```
  line 3: <algo_mode>        # FULL | SIMPLIFIED
```

### Padding

Все алгоритмы используют **PKCS#7 padding** для выравнивания данных до кратного размера блока. Padding снимается автоматически при расшифровании.

---

## Зависимости и запуск

### Системные зависимости

```bash
apt install libopenmpi-dev openmpi-bin libssl-dev
pip install flask gunicorn
```

### Компиляция CLI-бинарников

```bash
cd webapp

# Последовательные (зависят от OpenSSL)
gcc -O2 -o cli_wbc1_nomix    cli_wbc1_nomix.c    -lm -lssl -lcrypto
gcc -O2 -o cli_wbc2_serial   cli_wbc2_serial.c   -lm -lssl -lcrypto
gcc -O2 -o cli_wbc1_casc     cli_wbc1_casc.c     -lm -lssl -lcrypto
gcc -O2 -o cli_wbc1_casc2    cli_wbc1_casc2.c    -lm -lssl -lcrypto

# Параллельные (MPI + OpenSSL)
mpicc -O2 -o cli_pwbc0  cli_pwbc0.c  -lm -lssl -lcrypto
mpicc -O2 -o cli_pwbc1  cli_pwbc1.c  -lm -lssl -lcrypto
mpicc -O2 -o cli_pwbc2  cli_pwbc2.c  -lm -lssl -lcrypto
mpicc -O2 -o cli_pwbc21 cli_pwbc21.c -lm -lssl -lcrypto
```

### Запуск сервера

```bash
cd webapp
gunicorn --bind 0.0.0.0:5000 --workers 1 --timeout 300 app:app
```

Приложение доступно по адресу: `http://localhost:5000`

---

## Сравнение алгоритмов

| Алгоритм | Тип | Раунды | Параметры | Стойкость | Скорость |
|---|---|---|---|---|---|
| WBC1 | Послед. | 16 | enc_mode | ●●● | ●●●● |
| WBC2 | Послед. | 32 | enc_mode | ●●●● | ●●● |
| WBC1.0 | Послед. | 16 | enc_mode | ●●●● | ●●● |
| WBC1.1 | Послед. | 16×2 прохода | enc_mode | ●●●●● | ●● |
| PWBC1 | MPI | 32 | nprocs | ●●●● | ●●● |
| PWBC1.1 | MPI | 16 | shift_mode, nprocs | ●●● | ●●●● |
| PWBC2 | MPI | 1–100 | num_rounds, nprocs | ●●●●● | ●● |
| PWBC2.1 | MPI | 8/16 | algo_mode, nprocs | ●●●●/●●●● | ●●●●/●●● |
